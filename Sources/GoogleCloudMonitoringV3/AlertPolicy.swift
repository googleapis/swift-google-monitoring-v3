// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

import Foundation
import GoogleRpc
import GoogleType
@_spi(GoogleCloudInternal) import GoogleWKT

/// A description of the conditions under which some aspect of your system is
/// considered to be "unhealthy" and the ways to notify people or services about
/// this state. For an overview of alerting policies, see
/// [Introduction to Alerting](https://cloud.google.com/monitoring/alerts/).
///
public struct AlertPolicy: Codable, Equatable, GoogleWKT._AnyPackable,
  Sendable
{
  /// Identifier. Required if the policy exists. The resource name for this
  /// policy. The format is:
  ///
  ///     projects/[PROJECT_ID_OR_NUMBER]/alertPolicies/[ALERT_POLICY_ID]
  ///
  /// `[ALERT_POLICY_ID]` is assigned by Cloud Monitoring when the policy
  /// is created. When calling the
  /// [alertPolicies.create][google.monitoring.v3.AlertPolicyService.CreateAlertPolicy]
  /// method, do not include the `name` field in the alerting policy passed as
  /// part of the request.
  ///
  /// [google.monitoring.v3.AlertPolicyService.CreateAlertPolicy]: <doc:AlertPolicyServiceClient/createAlertPolicy(request:options:)>
  public var name: Swift.String = Swift.String()

  /// A short name or phrase used to identify the policy in dashboards,
  /// notifications, and incidents. To avoid confusion, don't use the same
  /// display name for multiple policies in the same project. The name is
  /// limited to 512 Unicode characters.
  ///
  /// The convention for the display_name of a PrometheusQueryLanguageCondition
  /// is "{rule group name}/{alert name}", where the {rule group name} and
  /// {alert name} should be taken from the corresponding Prometheus
  /// configuration file. This convention is not enforced.
  /// In any case the display_name is not a unique key of the AlertPolicy.
  public var displayName: Swift.String = Swift.String()

  /// Documentation that is included with notifications and incidents related to
  /// this policy. Best practice is for the documentation to include information
  /// to help responders understand, mitigate, escalate, and correct the
  /// underlying problems detected by the alerting policy. Notification channels
  /// that have limited capacity might not show this documentation.
  public var documentation: AlertPolicy.Documentation? = nil

  /// User-supplied key/value data to be used for organizing and
  /// identifying the `AlertPolicy` objects.
  ///
  /// The field can contain up to 64 entries. Each key and value is limited to
  /// 63 Unicode characters or 128 bytes, whichever is smaller. Labels and
  /// values can contain only lowercase letters, numerals, underscores, and
  /// dashes. Keys must begin with a letter.
  ///
  /// Note that Prometheus {alert name} is a
  /// [valid Prometheus label
  /// names](https://prometheus.io/docs/concepts/data_model/#metric-names-and-labels),
  /// whereas Prometheus {rule group} is an unrestricted UTF-8 string.
  /// This means that they cannot be stored as-is in user labels, because
  /// they may contain characters that are not allowed in user-label values.
  public var userLabels: [Swift.String: Swift.String] = [:]

  /// A list of conditions for the policy. The conditions are combined by AND or
  /// OR according to the `combiner` field. If the combined conditions evaluate
  /// to true, then an incident is created. A policy can have from one to six
  /// conditions.
  /// If `condition_time_series_query_language` is present, it must be the only
  /// `condition`.
  /// If `condition_monitoring_query_language` is present, it must be the only
  /// `condition`.
  public var conditions: [AlertPolicy.Condition] = []

  /// How to combine the results of multiple conditions to determine if an
  /// incident should be opened.
  /// If `condition_time_series_query_language` is present, this must be
  /// `COMBINE_UNSPECIFIED`.
  public var combiner: AlertPolicy.ConditionCombinerType = AlertPolicy.ConditionCombinerType()

  /// Whether or not the policy is enabled. On write, the default interpretation
  /// if unset is that the policy is enabled. On read, clients should not make
  /// any assumption about the state if it has not been populated. The
  /// field should always be populated on List and Get operations, unless
  /// a field projection has been specified that strips it out.
  public var enabled: GoogleWKT.BoolValue? = nil

  /// Read-only description of how the alerting policy is invalid. This field is
  /// only set when the alerting policy is invalid. An invalid alerting policy
  /// will not generate incidents.
  public var validity: GoogleRpc.Status? = nil

  /// Identifies the notification channels to which notifications should be sent
  /// when incidents are opened or closed or when new violations occur on
  /// an already opened incident. Each element of this array corresponds to
  /// the `name` field in each of the
  /// [`NotificationChannel`][google.monitoring.v3.NotificationChannel]
  /// objects that are returned from the [`ListNotificationChannels`]
  /// [google.monitoring.v3.NotificationChannelService.ListNotificationChannels]
  /// method. The format of the entries in this field is:
  ///
  ///     projects/[PROJECT_ID_OR_NUMBER]/notificationChannels/[CHANNEL_ID]
  ///
  /// [google.monitoring.v3.NotificationChannel]: <doc:NotificationChannel>
  public var notificationChannels: [Swift.String] = []

  /// A read-only record of the creation of the alerting policy. If provided
  /// in a call to create or update, this field will be ignored.
  public var creationRecord: MutationRecord? = nil

  /// A read-only record of the most recent change to the alerting policy. If
  /// provided in a call to create or update, this field will be ignored.
  public var mutationRecord: MutationRecord? = nil

  /// Control over how this alerting policy's notification channels are notified.
  public var alertStrategy: AlertPolicy.AlertStrategy? = nil

  /// Optional. The severity of an alerting policy indicates how important
  /// incidents generated by that policy are. The severity level will be
  /// displayed on the Incident detail page and in notifications.
  public var severity: AlertPolicy.Severity = AlertPolicy.Severity()

  @_spi(GoogleCloudInternal) public var _unknownFields: GoogleWKT._UnknownFields = .init()

  /// Initialize a new instance of `AlertPolicy`.
  public init() {}

  /// Use `config` to return a new instance of this object, with some fields updated.
  ///
  /// Commonly used to initialize the value, for example:
  ///
  /// ```
  /// let value = AlertPolicy().with { $0.name = ... }
  /// ```
  public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
    var copy = self
    try config(&copy)
    return copy
  }

  private struct CodingKeys: CodingKey {
    var stringValue: Swift.String
    var intValue: Swift.Int? { nil }
    init(stringValue: Swift.String) { self.stringValue = stringValue }
    init?(intValue: Swift.Int) { nil }

    static let name = CodingKeys(stringValue: "name")
    static let displayName = CodingKeys(stringValue: "displayName")
    static let documentation = CodingKeys(stringValue: "documentation")
    static let userLabels = CodingKeys(stringValue: "userLabels")
    static let conditions = CodingKeys(stringValue: "conditions")
    static let combiner = CodingKeys(stringValue: "combiner")
    static let enabled = CodingKeys(stringValue: "enabled")
    static let validity = CodingKeys(stringValue: "validity")
    static let notificationChannels = CodingKeys(stringValue: "notificationChannels")
    static let creationRecord = CodingKeys(stringValue: "creationRecord")
    static let mutationRecord = CodingKeys(stringValue: "mutationRecord")
    static let alertStrategy = CodingKeys(stringValue: "alertStrategy")
    static let severity = CodingKeys(stringValue: "severity")

    static let _knownKeys: Set<Swift.String> = [
      "name",
      "displayName",
      "documentation",
      "userLabels",
      "conditions",
      "combiner",
      "enabled",
      "validity",
      "notificationChannels",
      "creationRecord",
      "mutationRecord",
      "alertStrategy",
      "severity",
    ]
  }

  public init(from decoder: Decoder) throws {
    let container = try decoder.container(keyedBy: CodingKeys.self)
    if let value = try container.decodeIfPresent(Swift.String.self, forKey: .name) {
      self.name = value
    }
    if let value = try container.decodeIfPresent(Swift.String.self, forKey: .displayName) {
      self.displayName = value
    }
    self.documentation = try container.decodeIfPresent(
      AlertPolicy.Documentation.self, forKey: .documentation)
    if let value = try container.decodeIfPresent(
      [Swift.String: Swift.String].self, forKey: .userLabels)
    {
      self.userLabels = value
    }
    if let value = try container.decodeIfPresent([AlertPolicy.Condition].self, forKey: .conditions)
    {
      self.conditions = value
    }
    if let value = try container.decodeIfPresent(
      AlertPolicy.ConditionCombinerType.self, forKey: .combiner)
    {
      self.combiner = value
    }
    self.enabled = try container.decodeIfPresent(GoogleWKT.BoolValue.self, forKey: .enabled)
    self.validity = try container.decodeIfPresent(GoogleRpc.Status.self, forKey: .validity)
    if let value = try container.decodeIfPresent([Swift.String].self, forKey: .notificationChannels)
    {
      self.notificationChannels = value
    }
    self.creationRecord = try container.decodeIfPresent(
      MutationRecord.self, forKey: .creationRecord)
    self.mutationRecord = try container.decodeIfPresent(
      MutationRecord.self, forKey: .mutationRecord)
    self.alertStrategy = try container.decodeIfPresent(
      AlertPolicy.AlertStrategy.self, forKey: .alertStrategy)
    if let value = try container.decodeIfPresent(AlertPolicy.Severity.self, forKey: .severity) {
      self.severity = value
    }
    for key in container.allKeys where !CodingKeys._knownKeys.contains(key.stringValue) {
      self._unknownFields.json[key.stringValue] = try container.decode(
        GoogleWKT.Value.self, forKey: key)
    }
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.container(keyedBy: CodingKeys.self)
    try container.encode(self.name, forKey: .name)
    try container.encode(self.displayName, forKey: .displayName)
    try container.encodeIfPresent(self.documentation, forKey: .documentation)
    try container.encode(self.userLabels, forKey: .userLabels)
    try container.encode(self.conditions, forKey: .conditions)
    try container.encode(self.combiner, forKey: .combiner)
    try container.encodeIfPresent(self.enabled, forKey: .enabled)
    try container.encodeIfPresent(self.validity, forKey: .validity)
    try container.encode(self.notificationChannels, forKey: .notificationChannels)
    try container.encodeIfPresent(self.creationRecord, forKey: .creationRecord)
    try container.encodeIfPresent(self.mutationRecord, forKey: .mutationRecord)
    try container.encodeIfPresent(self.alertStrategy, forKey: .alertStrategy)
    try container.encode(self.severity, forKey: .severity)
    for (key, value) in self._unknownFields.json {
      try container.encode(value, forKey: CodingKeys(stringValue: key))
    }
  }

  /// Documentation that is included in the notifications and incidents
  /// pertaining to this policy.
  public struct Documentation: Codable, Equatable, GoogleWKT._AnyPackable,
    Sendable
  {
    /// The body of the documentation, interpreted according to `mime_type`.
    /// The content may not exceed 8,192 Unicode characters and may not exceed
    /// more than 10,240 bytes when encoded in UTF-8 format, whichever is
    /// smaller. This text can be [templatized by using
    /// variables](https://cloud.google.com/monitoring/alerts/doc-variables#doc-vars).
    public var content: Swift.String = Swift.String()

    /// The format of the `content` field. Presently, only the value
    /// `"text/markdown"` is supported. See
    /// [Markdown](https://en.wikipedia.org/wiki/Markdown) for more information.
    public var mimeType: Swift.String = Swift.String()

    /// Optional. The subject line of the notification. The subject line may not
    /// exceed 10,240 bytes. In notifications generated by this policy, the
    /// contents of the subject line after variable expansion will be truncated
    /// to 255 bytes or shorter at the latest UTF-8 character boundary. The
    /// 255-byte limit is recommended by [this
    /// thread](https://stackoverflow.com/questions/1592291/what-is-the-email-subject-length-limit).
    /// It is both the limit imposed by some third-party ticketing products and
    /// it is common to define textual fields in databases as VARCHAR(255).
    ///
    /// The contents of the subject line can be [templatized by using
    /// variables](https://cloud.google.com/monitoring/alerts/doc-variables#doc-vars).
    /// If this field is missing or empty, a default subject line will be
    /// generated.
    public var subject: Swift.String = Swift.String()

    /// Optional. Links to content such as playbooks, repositories, and other
    /// resources. This field can contain up to 3 entries.
    public var links: [AlertPolicy.Documentation.Link] = []

    @_spi(GoogleCloudInternal) public var _unknownFields: GoogleWKT._UnknownFields = .init()

    /// Initialize a new instance of `Documentation`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = Documentation().with { $0.content = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    private struct CodingKeys: CodingKey {
      var stringValue: Swift.String
      var intValue: Swift.Int? { nil }
      init(stringValue: Swift.String) { self.stringValue = stringValue }
      init?(intValue: Swift.Int) { nil }

      static let content = CodingKeys(stringValue: "content")
      static let mimeType = CodingKeys(stringValue: "mimeType")
      static let subject = CodingKeys(stringValue: "subject")
      static let links = CodingKeys(stringValue: "links")

      static let _knownKeys: Set<Swift.String> = [
        "content",
        "mimeType",
        "subject",
        "links",
      ]
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.container(keyedBy: CodingKeys.self)
      if let value = try container.decodeIfPresent(Swift.String.self, forKey: .content) {
        self.content = value
      }
      if let value = try container.decodeIfPresent(Swift.String.self, forKey: .mimeType) {
        self.mimeType = value
      }
      if let value = try container.decodeIfPresent(Swift.String.self, forKey: .subject) {
        self.subject = value
      }
      if let value = try container.decodeIfPresent(
        [AlertPolicy.Documentation.Link].self, forKey: .links)
      {
        self.links = value
      }
      for key in container.allKeys where !CodingKeys._knownKeys.contains(key.stringValue) {
        self._unknownFields.json[key.stringValue] = try container.decode(
          GoogleWKT.Value.self, forKey: key)
      }
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.container(keyedBy: CodingKeys.self)
      try container.encode(self.content, forKey: .content)
      try container.encode(self.mimeType, forKey: .mimeType)
      try container.encode(self.subject, forKey: .subject)
      try container.encode(self.links, forKey: .links)
      for (key, value) in self._unknownFields.json {
        try container.encode(value, forKey: CodingKeys(stringValue: key))
      }
    }

    /// Links to content such as playbooks, repositories, and other resources.
    public struct Link: Codable, Equatable, GoogleWKT._AnyPackable,
      Sendable
    {
      /// A short display name for the link. The display name must not be empty
      /// or exceed 63 characters. Example: "playbook".
      public var displayName: Swift.String = Swift.String()

      /// The url of a webpage.
      /// A url can be templatized by using variables
      /// in the path or the query parameters. The total length of a URL should
      /// not exceed 2083 characters before and after variable expansion.
      /// Example: "https://my_domain.com/playbook?name=${resource.name}"
      public var url: Swift.String = Swift.String()

      @_spi(GoogleCloudInternal) public var _unknownFields: GoogleWKT._UnknownFields = .init()

      /// Initialize a new instance of `Link`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = Link().with { $0.displayName = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      private struct CodingKeys: CodingKey {
        var stringValue: Swift.String
        var intValue: Swift.Int? { nil }
        init(stringValue: Swift.String) { self.stringValue = stringValue }
        init?(intValue: Swift.Int) { nil }

        static let displayName = CodingKeys(stringValue: "displayName")
        static let url = CodingKeys(stringValue: "url")

        static let _knownKeys: Set<Swift.String> = [
          "displayName",
          "url",
        ]
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        if let value = try container.decodeIfPresent(Swift.String.self, forKey: .displayName) {
          self.displayName = value
        }
        if let value = try container.decodeIfPresent(Swift.String.self, forKey: .url) {
          self.url = value
        }
        for key in container.allKeys where !CodingKeys._knownKeys.contains(key.stringValue) {
          self._unknownFields.json[key.stringValue] = try container.decode(
            GoogleWKT.Value.self, forKey: key)
        }
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.displayName, forKey: .displayName)
        try container.encode(self.url, forKey: .url)
        for (key, value) in self._unknownFields.json {
          try container.encode(value, forKey: CodingKeys(stringValue: key))
        }
      }

      public static var _anyTypeUrl: Swift.String {
        return "type.googleapis.com/google.monitoring.v3.AlertPolicy.Documentation.Link"
      }
      public init(fromAny any: GoogleWKT.`Any`) throws {
        self = try GoogleWKT._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleWKT.Struct {
        return try GoogleWKT._slowAnySerialize(message: self)
      }
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.monitoring.v3.AlertPolicy.Documentation"
    }
    public init(fromAny any: GoogleWKT.`Any`) throws {
      self = try GoogleWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleWKT.Struct {
      return try GoogleWKT._slowAnySerialize(message: self)
    }
  }

  /// A condition is a true/false test that determines when an alerting policy
  /// should open an incident. If a condition evaluates to true, it signifies
  /// that something is wrong.
  public struct Condition: Codable, Equatable, GoogleWKT._AnyPackable,
    Sendable
  {
    /// Required if the condition exists. The unique resource name for this
    /// condition. Its format is:
    ///
    ///     projects/[PROJECT_ID_OR_NUMBER]/alertPolicies/[POLICY_ID]/conditions/[CONDITION_ID]
    ///
    /// `[CONDITION_ID]` is assigned by Cloud Monitoring when the
    /// condition is created as part of a new or updated alerting policy.
    ///
    /// When calling the
    /// [alertPolicies.create][google.monitoring.v3.AlertPolicyService.CreateAlertPolicy]
    /// method, do not include the `name` field in the conditions of the
    /// requested alerting policy. Cloud Monitoring creates the
    /// condition identifiers and includes them in the new policy.
    ///
    /// When calling the
    /// [alertPolicies.update][google.monitoring.v3.AlertPolicyService.UpdateAlertPolicy]
    /// method to update a policy, including a condition `name` causes the
    /// existing condition to be updated. Conditions without names are added to
    /// the updated policy. Existing conditions are deleted if they are not
    /// updated.
    ///
    /// Best practice is to preserve `[CONDITION_ID]` if you make only small
    /// changes, such as those to condition thresholds, durations, or trigger
    /// values.  Otherwise, treat the change as a new condition and let the
    /// existing condition be deleted.
    ///
    /// [google.monitoring.v3.AlertPolicyService.CreateAlertPolicy]: <doc:AlertPolicyServiceClient/createAlertPolicy(request:options:)>
    /// [google.monitoring.v3.AlertPolicyService.UpdateAlertPolicy]: <doc:AlertPolicyServiceClient/updateAlertPolicy(request:options:)>
    public var name: Swift.String = Swift.String()

    /// A short name or phrase used to identify the condition in dashboards,
    /// notifications, and incidents. To avoid confusion, don't use the same
    /// display name for multiple conditions in the same policy.
    public var displayName: Swift.String = Swift.String()

    /// Only one of the following condition types will be specified.
    public var condition: OneOf_Condition? = nil

    @_spi(GoogleCloudInternal) public var _unknownFields: GoogleWKT._UnknownFields = .init()

    /// Initialize a new instance of `Condition`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = Condition().with { $0.name = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    private struct CodingKeys: CodingKey {
      var stringValue: Swift.String
      var intValue: Swift.Int? { nil }
      init(stringValue: Swift.String) { self.stringValue = stringValue }
      init?(intValue: Swift.Int) { nil }

      static let name = CodingKeys(stringValue: "name")
      static let displayName = CodingKeys(stringValue: "displayName")
      static let conditionThreshold = CodingKeys(stringValue: "conditionThreshold")
      static let conditionAbsent = CodingKeys(stringValue: "conditionAbsent")
      static let conditionMatchedLog = CodingKeys(stringValue: "conditionMatchedLog")
      static let conditionMonitoringQueryLanguage = CodingKeys(
        stringValue: "conditionMonitoringQueryLanguage")
      static let conditionPrometheusQueryLanguage = CodingKeys(
        stringValue: "conditionPrometheusQueryLanguage")
      static let conditionSql = CodingKeys(stringValue: "conditionSql")

      static let _knownKeys: Set<Swift.String> = [
        "name",
        "displayName",
        "conditionThreshold",
        "conditionAbsent",
        "conditionMatchedLog",
        "conditionMonitoringQueryLanguage",
        "conditionPrometheusQueryLanguage",
        "conditionSql",
      ]
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.container(keyedBy: CodingKeys.self)
      if let value = try container.decodeIfPresent(Swift.String.self, forKey: .name) {
        self.name = value
      }
      if let value = try container.decodeIfPresent(Swift.String.self, forKey: .displayName) {
        self.displayName = value
      }

      var condition: OneOf_Condition? = nil
      let conditionCheckAndSet = {
        if condition != nil {
          throw DecodingError.dataCorrupted(
            DecodingError.Context(
              codingPath: decoder.codingPath,
              debugDescription: "Multiple values set for oneof 'condition'"))
        }
        condition = $0
      }
      if let conditionThreshold = try container.decodeIfPresent(
        AlertPolicy.Condition.MetricThreshold?.self, forKey: .conditionThreshold)
      {
        try conditionCheckAndSet(.conditionThreshold(conditionThreshold))
      }
      if let conditionAbsent = try container.decodeIfPresent(
        AlertPolicy.Condition.MetricAbsence?.self, forKey: .conditionAbsent)
      {
        try conditionCheckAndSet(.conditionAbsent(conditionAbsent))
      }
      if let conditionMatchedLog = try container.decodeIfPresent(
        AlertPolicy.Condition.LogMatch?.self, forKey: .conditionMatchedLog)
      {
        try conditionCheckAndSet(.conditionMatchedLog(conditionMatchedLog))
      }
      if let conditionMonitoringQueryLanguage = try container.decodeIfPresent(
        AlertPolicy.Condition.MonitoringQueryLanguageCondition?.self,
        forKey: .conditionMonitoringQueryLanguage)
      {
        try conditionCheckAndSet(
          .conditionMonitoringQueryLanguage(conditionMonitoringQueryLanguage))
      }
      if let conditionPrometheusQueryLanguage = try container.decodeIfPresent(
        AlertPolicy.Condition.PrometheusQueryLanguageCondition?.self,
        forKey: .conditionPrometheusQueryLanguage)
      {
        try conditionCheckAndSet(
          .conditionPrometheusQueryLanguage(conditionPrometheusQueryLanguage))
      }
      if let conditionSql = try container.decodeIfPresent(
        AlertPolicy.Condition.SqlCondition?.self, forKey: .conditionSql)
      {
        try conditionCheckAndSet(.conditionSql(conditionSql))
      }
      self.condition = condition
      for key in container.allKeys where !CodingKeys._knownKeys.contains(key.stringValue) {
        self._unknownFields.json[key.stringValue] = try container.decode(
          GoogleWKT.Value.self, forKey: key)
      }
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.container(keyedBy: CodingKeys.self)
      try container.encode(self.name, forKey: .name)
      try container.encode(self.displayName, forKey: .displayName)

      if let choice = self.condition {
        switch choice {
        case .conditionThreshold(let value):
          try container.encode(value, forKey: .conditionThreshold)
        case .conditionAbsent(let value):
          try container.encode(value, forKey: .conditionAbsent)
        case .conditionMatchedLog(let value):
          try container.encode(value, forKey: .conditionMatchedLog)
        case .conditionMonitoringQueryLanguage(let value):
          try container.encode(value, forKey: .conditionMonitoringQueryLanguage)
        case .conditionPrometheusQueryLanguage(let value):
          try container.encode(value, forKey: .conditionPrometheusQueryLanguage)
        case .conditionSql(let value):
          try container.encode(value, forKey: .conditionSql)
        }
      }
      for (key, value) in self._unknownFields.json {
        try container.encode(value, forKey: CodingKeys(stringValue: key))
      }
    }

    /// Specifies how many time series must fail a predicate to trigger a
    /// condition. If not specified, then a `{count: 1}` trigger is used.
    public struct Trigger: Codable, Equatable, GoogleWKT._AnyPackable,
      Sendable
    {
      /// A type of trigger.
      public var type: OneOf_Type? = nil

      @_spi(GoogleCloudInternal) public var _unknownFields: GoogleWKT._UnknownFields = .init()

      /// Initialize a new instance of `Trigger`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = Trigger().with { $0.count = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      private struct CodingKeys: CodingKey {
        var stringValue: Swift.String
        var intValue: Swift.Int? { nil }
        init(stringValue: Swift.String) { self.stringValue = stringValue }
        init?(intValue: Swift.Int) { nil }

        static let count = CodingKeys(stringValue: "count")
        static let percent = CodingKeys(stringValue: "percent")

        static let _knownKeys: Set<Swift.String> = [
          "count",
          "percent",
        ]
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        var type: OneOf_Type? = nil
        let typeCheckAndSet = {
          if type != nil {
            throw DecodingError.dataCorrupted(
              DecodingError.Context(
                codingPath: decoder.codingPath,
                debugDescription: "Multiple values set for oneof 'type'"))
          }
          type = $0
        }
        if let count = try container.decodeIfPresent(Swift.Int32.self, forKey: .count) {
          try typeCheckAndSet(.count(count))
        }
        if let percent = try container.decodeIfPresent(Swift.Double.self, forKey: .percent) {
          try typeCheckAndSet(.percent(percent))
        }
        self.type = type
        for key in container.allKeys where !CodingKeys._knownKeys.contains(key.stringValue) {
          self._unknownFields.json[key.stringValue] = try container.decode(
            GoogleWKT.Value.self, forKey: key)
        }
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)

        if let choice = self.type {
          switch choice {
          case .count(let value):
            try container.encode(value, forKey: .count)
          case .percent(let value):
            try container.encode(value, forKey: .percent)
          }
        }
        for (key, value) in self._unknownFields.json {
          try container.encode(value, forKey: CodingKeys(stringValue: key))
        }
      }

      /// A type of trigger.
      public enum OneOf_Type: Codable, Equatable, Sendable {
        /// The absolute number of time series that must fail
        /// the predicate for the condition to be triggered.
        case count(Swift.Int32)
        /// The percentage of time series that must fail the
        /// predicate for the condition to be triggered.
        case percent(Swift.Double)
      }

      public static var _anyTypeUrl: Swift.String {
        return "type.googleapis.com/google.monitoring.v3.AlertPolicy.Condition.Trigger"
      }
      public init(fromAny any: GoogleWKT.`Any`) throws {
        self = try GoogleWKT._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleWKT.Struct {
        return try GoogleWKT._slowAnySerialize(message: self)
      }
    }

    /// A condition type that compares a collection of time series
    /// against a threshold.
    public struct MetricThreshold: Codable, Equatable, GoogleWKT._AnyPackable,
      Sendable
    {
      /// Required. A
      /// [filter](https://cloud.google.com/monitoring/api/v3/filters) that
      /// identifies which time series should be compared with the threshold.
      ///
      /// The filter is similar to the one that is specified in the
      /// [`ListTimeSeries`
      /// request](https://cloud.google.com/monitoring/api/ref_v3/rest/v3/projects.timeSeries/list)
      /// (that call is useful to verify the time series that will be retrieved /
      /// processed). The filter must specify the metric type and the resource
      /// type. Optionally, it can specify resource labels and metric labels.
      /// This field must not exceed 2048 Unicode characters in length.
      public var filter: Swift.String = Swift.String()

      /// Specifies the alignment of data points in individual time series as
      /// well as how to combine the retrieved time series together (such as
      /// when aggregating multiple streams on each resource to a single
      /// stream for each resource or when aggregating streams across all
      /// members of a group of resources). Multiple aggregations
      /// are applied in the order specified.
      ///
      /// This field is similar to the one in the [`ListTimeSeries`
      /// request](https://cloud.google.com/monitoring/api/ref_v3/rest/v3/projects.timeSeries/list).
      /// It is advisable to use the `ListTimeSeries` method when debugging this
      /// field.
      public var aggregations: [Aggregation] = []

      /// A [filter](https://cloud.google.com/monitoring/api/v3/filters) that
      /// identifies a time series that should be used as the denominator of a
      /// ratio that will be compared with the threshold. If a
      /// `denominator_filter` is specified, the time series specified by the
      /// `filter` field will be used as the numerator.
      ///
      /// The filter must specify the metric type and optionally may contain
      /// restrictions on resource type, resource labels, and metric labels.
      /// This field may not exceed 2048 Unicode characters in length.
      public var denominatorFilter: Swift.String = Swift.String()

      /// Specifies the alignment of data points in individual time series
      /// selected by `denominatorFilter` as
      /// well as how to combine the retrieved time series together (such as
      /// when aggregating multiple streams on each resource to a single
      /// stream for each resource or when aggregating streams across all
      /// members of a group of resources).
      ///
      /// When computing ratios, the `aggregations` and
      /// `denominator_aggregations` fields must use the same alignment period
      /// and produce time series that have the same periodicity and labels.
      public var denominatorAggregations: [Aggregation] = []

      /// When this field is present, the `MetricThreshold` condition forecasts
      /// whether the time series is predicted to violate the threshold within
      /// the `forecast_horizon`. When this field is not set, the
      /// `MetricThreshold` tests the current value of the timeseries against the
      /// threshold.
      public var forecastOptions: AlertPolicy.Condition.MetricThreshold.ForecastOptions? = nil

      /// The comparison to apply between the time series (indicated by `filter`
      /// and `aggregation`) and the threshold (indicated by `threshold_value`).
      /// The comparison is applied on each time series, with the time series
      /// on the left-hand side and the threshold on the right-hand side.
      ///
      /// Only `COMPARISON_LT` and `COMPARISON_GT` are supported currently.
      public var comparison: ComparisonType = ComparisonType()

      /// A value against which to compare the time series.
      public var thresholdValue: Swift.Double = Swift.Double()

      /// The amount of time that a time series must violate the
      /// threshold to be considered failing. Currently, only values
      /// that are a multiple of a minute--e.g., 0, 60, 120, or 300
      /// seconds--are supported. If an invalid value is given, an
      /// error will be returned. When choosing a duration, it is useful to
      /// keep in mind the frequency of the underlying time series data
      /// (which may also be affected by any alignments specified in the
      /// `aggregations` field); a good duration is long enough so that a single
      /// outlier does not generate spurious alerts, but short enough that
      /// unhealthy states are detected and alerted on quickly.
      public var duration: GoogleWKT.Duration? = nil

      /// The number/percent of time series for which the comparison must hold
      /// in order for the condition to trigger. If unspecified, then the
      /// condition will trigger if the comparison is true for any of the
      /// time series that have been identified by `filter` and `aggregations`,
      /// or by the ratio, if `denominator_filter` and `denominator_aggregations`
      /// are specified.
      public var trigger: AlertPolicy.Condition.Trigger? = nil

      /// A condition control that determines how metric-threshold conditions
      /// are evaluated when data stops arriving. To use this control, the value
      /// of the `duration` field must be greater than or equal to 60 seconds.
      public var evaluationMissingData: AlertPolicy.Condition.EvaluationMissingData = AlertPolicy
        .Condition.EvaluationMissingData()

      @_spi(GoogleCloudInternal) public var _unknownFields: GoogleWKT._UnknownFields = .init()

      /// Initialize a new instance of `MetricThreshold`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = MetricThreshold().with { $0.filter = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      private struct CodingKeys: CodingKey {
        var stringValue: Swift.String
        var intValue: Swift.Int? { nil }
        init(stringValue: Swift.String) { self.stringValue = stringValue }
        init?(intValue: Swift.Int) { nil }

        static let filter = CodingKeys(stringValue: "filter")
        static let aggregations = CodingKeys(stringValue: "aggregations")
        static let denominatorFilter = CodingKeys(stringValue: "denominatorFilter")
        static let denominatorAggregations = CodingKeys(stringValue: "denominatorAggregations")
        static let forecastOptions = CodingKeys(stringValue: "forecastOptions")
        static let comparison = CodingKeys(stringValue: "comparison")
        static let thresholdValue = CodingKeys(stringValue: "thresholdValue")
        static let duration = CodingKeys(stringValue: "duration")
        static let trigger = CodingKeys(stringValue: "trigger")
        static let evaluationMissingData = CodingKeys(stringValue: "evaluationMissingData")

        static let _knownKeys: Set<Swift.String> = [
          "filter",
          "aggregations",
          "denominatorFilter",
          "denominatorAggregations",
          "forecastOptions",
          "comparison",
          "thresholdValue",
          "duration",
          "trigger",
          "evaluationMissingData",
        ]
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        if let value = try container.decodeIfPresent(Swift.String.self, forKey: .filter) {
          self.filter = value
        }
        if let value = try container.decodeIfPresent([Aggregation].self, forKey: .aggregations) {
          self.aggregations = value
        }
        if let value = try container.decodeIfPresent(Swift.String.self, forKey: .denominatorFilter)
        {
          self.denominatorFilter = value
        }
        if let value = try container.decodeIfPresent(
          [Aggregation].self, forKey: .denominatorAggregations)
        {
          self.denominatorAggregations = value
        }
        self.forecastOptions = try container.decodeIfPresent(
          AlertPolicy.Condition.MetricThreshold.ForecastOptions.self, forKey: .forecastOptions)
        if let value = try container.decodeIfPresent(ComparisonType.self, forKey: .comparison) {
          self.comparison = value
        }
        if let value = try container.decodeIfPresent(Swift.Double.self, forKey: .thresholdValue) {
          self.thresholdValue = value
        }
        self.duration = try container.decodeIfPresent(GoogleWKT.Duration.self, forKey: .duration)
        self.trigger = try container.decodeIfPresent(
          AlertPolicy.Condition.Trigger.self, forKey: .trigger)
        if let value = try container.decodeIfPresent(
          AlertPolicy.Condition.EvaluationMissingData.self, forKey: .evaluationMissingData)
        {
          self.evaluationMissingData = value
        }
        for key in container.allKeys where !CodingKeys._knownKeys.contains(key.stringValue) {
          self._unknownFields.json[key.stringValue] = try container.decode(
            GoogleWKT.Value.self, forKey: key)
        }
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.filter, forKey: .filter)
        try container.encode(self.aggregations, forKey: .aggregations)
        try container.encode(self.denominatorFilter, forKey: .denominatorFilter)
        try container.encode(self.denominatorAggregations, forKey: .denominatorAggregations)
        try container.encodeIfPresent(self.forecastOptions, forKey: .forecastOptions)
        try container.encode(self.comparison, forKey: .comparison)
        try container.encode(self.thresholdValue, forKey: .thresholdValue)
        try container.encodeIfPresent(self.duration, forKey: .duration)
        try container.encodeIfPresent(self.trigger, forKey: .trigger)
        try container.encode(self.evaluationMissingData, forKey: .evaluationMissingData)
        for (key, value) in self._unknownFields.json {
          try container.encode(value, forKey: CodingKeys(stringValue: key))
        }
      }

      /// Options used when forecasting the time series and testing
      /// the predicted value against the threshold.
      public struct ForecastOptions: Codable, Equatable, GoogleWKT._AnyPackable,
        Sendable
      {
        /// Required. The length of time into the future to forecast whether a
        /// time series will violate the threshold. If the predicted value is
        /// found to violate the threshold, and the violation is observed in all
        /// forecasts made for the configured `duration`, then the time series is
        /// considered to be failing.
        /// The forecast horizon can range from 1 hour to 60 hours.
        public var forecastHorizon: GoogleWKT.Duration? = nil

        @_spi(GoogleCloudInternal) public var _unknownFields: GoogleWKT._UnknownFields = .init()

        /// Initialize a new instance of `ForecastOptions`.
        public init() {}

        /// Use `config` to return a new instance of this object, with some fields updated.
        ///
        /// Commonly used to initialize the value, for example:
        ///
        /// ```
        /// let value = ForecastOptions().with { $0.forecastHorizon = ... }
        /// ```
        public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
          var copy = self
          try config(&copy)
          return copy
        }

        private struct CodingKeys: CodingKey {
          var stringValue: Swift.String
          var intValue: Swift.Int? { nil }
          init(stringValue: Swift.String) { self.stringValue = stringValue }
          init?(intValue: Swift.Int) { nil }

          static let forecastHorizon = CodingKeys(stringValue: "forecastHorizon")

          static let _knownKeys: Set<Swift.String> = [
            "forecastHorizon"
          ]
        }

        public init(from decoder: Decoder) throws {
          let container = try decoder.container(keyedBy: CodingKeys.self)
          self.forecastHorizon = try container.decodeIfPresent(
            GoogleWKT.Duration.self, forKey: .forecastHorizon)
          for key in container.allKeys where !CodingKeys._knownKeys.contains(key.stringValue) {
            self._unknownFields.json[key.stringValue] = try container.decode(
              GoogleWKT.Value.self, forKey: key)
          }
        }

        public func encode(to encoder: Encoder) throws {
          var container = encoder.container(keyedBy: CodingKeys.self)
          try container.encodeIfPresent(self.forecastHorizon, forKey: .forecastHorizon)
          for (key, value) in self._unknownFields.json {
            try container.encode(value, forKey: CodingKeys(stringValue: key))
          }
        }

        public static var _anyTypeUrl: Swift.String {
          return
            "type.googleapis.com/google.monitoring.v3.AlertPolicy.Condition.MetricThreshold.ForecastOptions"
        }
        public init(fromAny any: GoogleWKT.`Any`) throws {
          self = try GoogleWKT._slowAnyDeserialize(Self.self, from: any)
        }
        public func _pack() throws -> GoogleWKT.Struct {
          return try GoogleWKT._slowAnySerialize(message: self)
        }
      }

      public static var _anyTypeUrl: Swift.String {
        return "type.googleapis.com/google.monitoring.v3.AlertPolicy.Condition.MetricThreshold"
      }
      public init(fromAny any: GoogleWKT.`Any`) throws {
        self = try GoogleWKT._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleWKT.Struct {
        return try GoogleWKT._slowAnySerialize(message: self)
      }
    }

    /// A condition type that checks that monitored resources
    /// are reporting data. The configuration defines a metric and
    /// a set of monitored resources. The predicate is considered in violation
    /// when a time series for the specified metric of a monitored
    /// resource does not include any data in the specified `duration`.
    public struct MetricAbsence: Codable, Equatable, GoogleWKT._AnyPackable,
      Sendable
    {
      /// Required. A
      /// [filter](https://cloud.google.com/monitoring/api/v3/filters) that
      /// identifies which time series should be compared with the threshold.
      ///
      /// The filter is similar to the one that is specified in the
      /// [`ListTimeSeries`
      /// request](https://cloud.google.com/monitoring/api/ref_v3/rest/v3/projects.timeSeries/list)
      /// (that call is useful to verify the time series that will be retrieved /
      /// processed). The filter must specify the metric type and the resource
      /// type. Optionally, it can specify resource labels and metric labels.
      /// This field must not exceed 2048 Unicode characters in length.
      public var filter: Swift.String = Swift.String()

      /// Specifies the alignment of data points in individual time series as
      /// well as how to combine the retrieved time series together (such as
      /// when aggregating multiple streams on each resource to a single
      /// stream for each resource or when aggregating streams across all
      /// members of a group of resources). Multiple aggregations
      /// are applied in the order specified.
      ///
      /// This field is similar to the one in the [`ListTimeSeries`
      /// request](https://cloud.google.com/monitoring/api/ref_v3/rest/v3/projects.timeSeries/list).
      /// It is advisable to use the `ListTimeSeries` method when debugging this
      /// field.
      public var aggregations: [Aggregation] = []

      /// The amount of time that a time series must fail to report new
      /// data to be considered failing. The minimum value of this field
      /// is 120 seconds. Larger values that are a multiple of a
      /// minute--for example, 240 or 300 seconds--are supported.
      /// If an invalid value is given, an
      /// error will be returned. The `Duration.nanos` field is
      /// ignored.
      public var duration: GoogleWKT.Duration? = nil

      /// The number/percent of time series for which the comparison must hold
      /// in order for the condition to trigger. If unspecified, then the
      /// condition will trigger if the comparison is true for any of the
      /// time series that have been identified by `filter` and `aggregations`.
      public var trigger: AlertPolicy.Condition.Trigger? = nil

      @_spi(GoogleCloudInternal) public var _unknownFields: GoogleWKT._UnknownFields = .init()

      /// Initialize a new instance of `MetricAbsence`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = MetricAbsence().with { $0.filter = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      private struct CodingKeys: CodingKey {
        var stringValue: Swift.String
        var intValue: Swift.Int? { nil }
        init(stringValue: Swift.String) { self.stringValue = stringValue }
        init?(intValue: Swift.Int) { nil }

        static let filter = CodingKeys(stringValue: "filter")
        static let aggregations = CodingKeys(stringValue: "aggregations")
        static let duration = CodingKeys(stringValue: "duration")
        static let trigger = CodingKeys(stringValue: "trigger")

        static let _knownKeys: Set<Swift.String> = [
          "filter",
          "aggregations",
          "duration",
          "trigger",
        ]
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        if let value = try container.decodeIfPresent(Swift.String.self, forKey: .filter) {
          self.filter = value
        }
        if let value = try container.decodeIfPresent([Aggregation].self, forKey: .aggregations) {
          self.aggregations = value
        }
        self.duration = try container.decodeIfPresent(GoogleWKT.Duration.self, forKey: .duration)
        self.trigger = try container.decodeIfPresent(
          AlertPolicy.Condition.Trigger.self, forKey: .trigger)
        for key in container.allKeys where !CodingKeys._knownKeys.contains(key.stringValue) {
          self._unknownFields.json[key.stringValue] = try container.decode(
            GoogleWKT.Value.self, forKey: key)
        }
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.filter, forKey: .filter)
        try container.encode(self.aggregations, forKey: .aggregations)
        try container.encodeIfPresent(self.duration, forKey: .duration)
        try container.encodeIfPresent(self.trigger, forKey: .trigger)
        for (key, value) in self._unknownFields.json {
          try container.encode(value, forKey: CodingKeys(stringValue: key))
        }
      }

      public static var _anyTypeUrl: Swift.String {
        return "type.googleapis.com/google.monitoring.v3.AlertPolicy.Condition.MetricAbsence"
      }
      public init(fromAny any: GoogleWKT.`Any`) throws {
        self = try GoogleWKT._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleWKT.Struct {
        return try GoogleWKT._slowAnySerialize(message: self)
      }
    }

    /// A condition type that checks whether a log message in the [scoping
    /// project](https://cloud.google.com/monitoring/api/v3#project_name)
    /// satisfies the given filter. Logs from other projects in the metrics
    /// scope are not evaluated.
    public struct LogMatch: Codable, Equatable, GoogleWKT._AnyPackable,
      Sendable
    {
      /// Required. A logs-based filter. See [Advanced Logs
      /// Queries](https://cloud.google.com/logging/docs/view/advanced-queries)
      /// for how this filter should be constructed.
      public var filter: Swift.String = Swift.String()

      /// Optional. A map from a label key to an extractor expression, which is
      /// used to extract the value for this label key. Each entry in this map is
      /// a specification for how data should be extracted from log entries that
      /// match `filter`. Each combination of extracted values is treated as a
      /// separate rule for the purposes of triggering notifications. Label keys
      /// and corresponding values can be used in notifications generated by this
      /// condition.
      ///
      /// Please see [the documentation on logs-based metric
      /// `valueExtractor`s](https://cloud.google.com/logging/docs/reference/v2/rest/v2/projects.metrics#LogMetric.FIELDS.value_extractor)
      /// for syntax and examples.
      public var labelExtractors: [Swift.String: Swift.String] = [:]

      @_spi(GoogleCloudInternal) public var _unknownFields: GoogleWKT._UnknownFields = .init()

      /// Initialize a new instance of `LogMatch`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = LogMatch().with { $0.filter = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      private struct CodingKeys: CodingKey {
        var stringValue: Swift.String
        var intValue: Swift.Int? { nil }
        init(stringValue: Swift.String) { self.stringValue = stringValue }
        init?(intValue: Swift.Int) { nil }

        static let filter = CodingKeys(stringValue: "filter")
        static let labelExtractors = CodingKeys(stringValue: "labelExtractors")

        static let _knownKeys: Set<Swift.String> = [
          "filter",
          "labelExtractors",
        ]
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        if let value = try container.decodeIfPresent(Swift.String.self, forKey: .filter) {
          self.filter = value
        }
        if let value = try container.decodeIfPresent(
          [Swift.String: Swift.String].self, forKey: .labelExtractors)
        {
          self.labelExtractors = value
        }
        for key in container.allKeys where !CodingKeys._knownKeys.contains(key.stringValue) {
          self._unknownFields.json[key.stringValue] = try container.decode(
            GoogleWKT.Value.self, forKey: key)
        }
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.filter, forKey: .filter)
        try container.encode(self.labelExtractors, forKey: .labelExtractors)
        for (key, value) in self._unknownFields.json {
          try container.encode(value, forKey: CodingKeys(stringValue: key))
        }
      }

      public static var _anyTypeUrl: Swift.String {
        return "type.googleapis.com/google.monitoring.v3.AlertPolicy.Condition.LogMatch"
      }
      public init(fromAny any: GoogleWKT.`Any`) throws {
        self = try GoogleWKT._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleWKT.Struct {
        return try GoogleWKT._slowAnySerialize(message: self)
      }
    }

    /// A condition type that allows alerting policies to be defined using
    /// [Monitoring Query Language](https://cloud.google.com/monitoring/mql).
    public struct MonitoringQueryLanguageCondition: Codable, Equatable, GoogleWKT._AnyPackable,
      Sendable
    {
      /// [Monitoring Query Language](https://cloud.google.com/monitoring/mql)
      /// query that outputs a boolean stream.
      public var query: Swift.String = Swift.String()

      /// The amount of time that a time series must violate the
      /// threshold to be considered failing. Currently, only values
      /// that are a multiple of a minute--e.g., 0, 60, 120, or 300
      /// seconds--are supported. If an invalid value is given, an
      /// error will be returned. When choosing a duration, it is useful to
      /// keep in mind the frequency of the underlying time series data
      /// (which may also be affected by any alignments specified in the
      /// `aggregations` field); a good duration is long enough so that a single
      /// outlier does not generate spurious alerts, but short enough that
      /// unhealthy states are detected and alerted on quickly.
      public var duration: GoogleWKT.Duration? = nil

      /// The number/percent of time series for which the comparison must hold
      /// in order for the condition to trigger. If unspecified, then the
      /// condition will trigger if the comparison is true for any of the
      /// time series that have been identified by `filter` and `aggregations`,
      /// or by the ratio, if `denominator_filter` and `denominator_aggregations`
      /// are specified.
      public var trigger: AlertPolicy.Condition.Trigger? = nil

      /// A condition control that determines how metric-threshold conditions
      /// are evaluated when data stops arriving.
      public var evaluationMissingData: AlertPolicy.Condition.EvaluationMissingData = AlertPolicy
        .Condition.EvaluationMissingData()

      @_spi(GoogleCloudInternal) public var _unknownFields: GoogleWKT._UnknownFields = .init()

      /// Initialize a new instance of `MonitoringQueryLanguageCondition`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = MonitoringQueryLanguageCondition().with { $0.query = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      private struct CodingKeys: CodingKey {
        var stringValue: Swift.String
        var intValue: Swift.Int? { nil }
        init(stringValue: Swift.String) { self.stringValue = stringValue }
        init?(intValue: Swift.Int) { nil }

        static let query = CodingKeys(stringValue: "query")
        static let duration = CodingKeys(stringValue: "duration")
        static let trigger = CodingKeys(stringValue: "trigger")
        static let evaluationMissingData = CodingKeys(stringValue: "evaluationMissingData")

        static let _knownKeys: Set<Swift.String> = [
          "query",
          "duration",
          "trigger",
          "evaluationMissingData",
        ]
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        if let value = try container.decodeIfPresent(Swift.String.self, forKey: .query) {
          self.query = value
        }
        self.duration = try container.decodeIfPresent(GoogleWKT.Duration.self, forKey: .duration)
        self.trigger = try container.decodeIfPresent(
          AlertPolicy.Condition.Trigger.self, forKey: .trigger)
        if let value = try container.decodeIfPresent(
          AlertPolicy.Condition.EvaluationMissingData.self, forKey: .evaluationMissingData)
        {
          self.evaluationMissingData = value
        }
        for key in container.allKeys where !CodingKeys._knownKeys.contains(key.stringValue) {
          self._unknownFields.json[key.stringValue] = try container.decode(
            GoogleWKT.Value.self, forKey: key)
        }
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.query, forKey: .query)
        try container.encodeIfPresent(self.duration, forKey: .duration)
        try container.encodeIfPresent(self.trigger, forKey: .trigger)
        try container.encode(self.evaluationMissingData, forKey: .evaluationMissingData)
        for (key, value) in self._unknownFields.json {
          try container.encode(value, forKey: CodingKeys(stringValue: key))
        }
      }

      public static var _anyTypeUrl: Swift.String {
        return
          "type.googleapis.com/google.monitoring.v3.AlertPolicy.Condition.MonitoringQueryLanguageCondition"
      }
      public init(fromAny any: GoogleWKT.`Any`) throws {
        self = try GoogleWKT._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleWKT.Struct {
        return try GoogleWKT._slowAnySerialize(message: self)
      }
    }

    /// A condition type that allows alerting policies to be defined using
    /// [Prometheus Query Language
    /// (PromQL)](https://prometheus.io/docs/prometheus/latest/querying/basics/).
    ///
    /// The PrometheusQueryLanguageCondition message contains information
    /// from a Prometheus alerting rule and its associated rule group.
    ///
    /// A Prometheus alerting rule is described
    /// [here](https://prometheus.io/docs/prometheus/latest/configuration/alerting_rules/).
    /// The semantics of a Prometheus alerting rule is described
    /// [here](https://prometheus.io/docs/prometheus/latest/configuration/recording_rules/#rule).
    ///
    /// A Prometheus rule group is described
    /// [here](https://prometheus.io/docs/prometheus/latest/configuration/recording_rules/).
    /// The semantics of a Prometheus rule group is described
    /// [here](https://prometheus.io/docs/prometheus/latest/configuration/recording_rules/#rule_group).
    ///
    /// Because Cloud Alerting has no representation of a Prometheus rule
    /// group resource, we must embed the information of the parent rule
    /// group inside each of the conditions that refer to it. We must also
    /// update the contents of all Prometheus alerts in case the information
    /// of their rule group changes.
    ///
    /// The PrometheusQueryLanguageCondition protocol buffer combines the
    /// information of the corresponding rule group and alerting rule.
    /// The structure of the PrometheusQueryLanguageCondition protocol buffer
    /// does NOT mimic the structure of the Prometheus rule group and alerting
    /// rule YAML declarations. The PrometheusQueryLanguageCondition protocol
    /// buffer may change in the future to support future rule group and/or
    /// alerting rule features. There are no new such features at the present
    /// time (2023-06-26).
    public struct PrometheusQueryLanguageCondition: Codable, Equatable, GoogleWKT._AnyPackable,
      Sendable
    {
      /// Required. The PromQL expression to evaluate. Every evaluation cycle
      /// this expression is evaluated at the current time, and all resultant
      /// time series become pending/firing alerts. This field must not be empty.
      public var query: Swift.String = Swift.String()

      /// Optional. Alerts are considered firing once their PromQL expression was
      /// evaluated to be "true" for this long.
      /// Alerts whose PromQL expression was not evaluated to be "true" for
      /// long enough are considered pending.
      /// Must be a non-negative duration or missing.
      /// This field is optional. Its default value is zero.
      public var duration: GoogleWKT.Duration? = nil

      /// Optional. How often this rule should be evaluated.
      /// Must be a positive multiple of 30 seconds or missing.
      /// This field is optional. Its default value is 30 seconds.
      /// If this PrometheusQueryLanguageCondition was generated from a
      /// Prometheus alerting rule, then this value should be taken from the
      /// enclosing rule group.
      public var evaluationInterval: GoogleWKT.Duration? = nil

      /// Optional. Labels to add to or overwrite in the PromQL query result.
      /// Label names [must be
      /// valid](https://prometheus.io/docs/concepts/data_model/#metric-names-and-labels).
      /// Label values can be [templatized by using
      /// variables](https://cloud.google.com/monitoring/alerts/doc-variables#doc-vars).
      /// The only available variable names are the names of the labels in the
      /// PromQL result, including "__name__" and "value". "labels" may be empty.
      public var labels: [Swift.String: Swift.String] = [:]

      /// Optional. The rule group name of this alert in the corresponding
      /// Prometheus configuration file.
      ///
      /// Some external tools may require this field to be populated correctly
      /// in order to refer to the original Prometheus configuration file.
      /// The rule group name and the alert name are necessary to update the
      /// relevant AlertPolicies in case the definition of the rule group changes
      /// in the future.
      ///
      /// This field is optional. If this field is not empty, then it must
      /// contain a valid UTF-8 string.
      /// This field may not exceed 2048 Unicode characters in length.
      public var ruleGroup: Swift.String = Swift.String()

      /// Optional. The alerting rule name of this alert in the corresponding
      /// Prometheus configuration file.
      ///
      /// Some external tools may require this field to be populated correctly
      /// in order to refer to the original Prometheus configuration file.
      /// The rule group name and the alert name are necessary to update the
      /// relevant AlertPolicies in case the definition of the rule group changes
      /// in the future.
      ///
      /// This field is optional. If this field is not empty, then it must be a
      /// [valid Prometheus label
      /// name](https://prometheus.io/docs/concepts/data_model/#metric-names-and-labels).
      /// This field may not exceed 2048 Unicode characters in length.
      public var alertRule: Swift.String = Swift.String()

      /// Optional. Whether to disable metric existence validation for this
      /// condition.
      ///
      /// This allows alerting policies to be defined on metrics that do not yet
      /// exist, improving advanced customer workflows such as configuring
      /// alerting policies using Terraform.
      ///
      /// Users with the `monitoring.alertPolicyViewer` role are able to see the
      /// name of the non-existent metric in the alerting policy condition.
      public var disableMetricValidation: Swift.Bool = Swift.Bool()

      @_spi(GoogleCloudInternal) public var _unknownFields: GoogleWKT._UnknownFields = .init()

      /// Initialize a new instance of `PrometheusQueryLanguageCondition`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = PrometheusQueryLanguageCondition().with { $0.query = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      private struct CodingKeys: CodingKey {
        var stringValue: Swift.String
        var intValue: Swift.Int? { nil }
        init(stringValue: Swift.String) { self.stringValue = stringValue }
        init?(intValue: Swift.Int) { nil }

        static let query = CodingKeys(stringValue: "query")
        static let duration = CodingKeys(stringValue: "duration")
        static let evaluationInterval = CodingKeys(stringValue: "evaluationInterval")
        static let labels = CodingKeys(stringValue: "labels")
        static let ruleGroup = CodingKeys(stringValue: "ruleGroup")
        static let alertRule = CodingKeys(stringValue: "alertRule")
        static let disableMetricValidation = CodingKeys(stringValue: "disableMetricValidation")

        static let _knownKeys: Set<Swift.String> = [
          "query",
          "duration",
          "evaluationInterval",
          "labels",
          "ruleGroup",
          "alertRule",
          "disableMetricValidation",
        ]
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        if let value = try container.decodeIfPresent(Swift.String.self, forKey: .query) {
          self.query = value
        }
        self.duration = try container.decodeIfPresent(GoogleWKT.Duration.self, forKey: .duration)
        self.evaluationInterval = try container.decodeIfPresent(
          GoogleWKT.Duration.self, forKey: .evaluationInterval)
        if let value = try container.decodeIfPresent(
          [Swift.String: Swift.String].self, forKey: .labels)
        {
          self.labels = value
        }
        if let value = try container.decodeIfPresent(Swift.String.self, forKey: .ruleGroup) {
          self.ruleGroup = value
        }
        if let value = try container.decodeIfPresent(Swift.String.self, forKey: .alertRule) {
          self.alertRule = value
        }
        if let value = try container.decodeIfPresent(
          Swift.Bool.self, forKey: .disableMetricValidation)
        {
          self.disableMetricValidation = value
        }
        for key in container.allKeys where !CodingKeys._knownKeys.contains(key.stringValue) {
          self._unknownFields.json[key.stringValue] = try container.decode(
            GoogleWKT.Value.self, forKey: key)
        }
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.query, forKey: .query)
        try container.encodeIfPresent(self.duration, forKey: .duration)
        try container.encodeIfPresent(self.evaluationInterval, forKey: .evaluationInterval)
        try container.encode(self.labels, forKey: .labels)
        try container.encode(self.ruleGroup, forKey: .ruleGroup)
        try container.encode(self.alertRule, forKey: .alertRule)
        try container.encode(self.disableMetricValidation, forKey: .disableMetricValidation)
        for (key, value) in self._unknownFields.json {
          try container.encode(value, forKey: CodingKeys(stringValue: key))
        }
      }

      public static var _anyTypeUrl: Swift.String {
        return
          "type.googleapis.com/google.monitoring.v3.AlertPolicy.Condition.PrometheusQueryLanguageCondition"
      }
      public init(fromAny any: GoogleWKT.`Any`) throws {
        self = try GoogleWKT._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleWKT.Struct {
        return try GoogleWKT._slowAnySerialize(message: self)
      }
    }

    /// A condition that allows alerting policies to be defined using GoogleSQL.
    /// SQL conditions examine a sliding window of logs using GoogleSQL.
    /// Alert policies with SQL conditions may incur additional billing.
    public struct SqlCondition: Codable, Equatable, GoogleWKT._AnyPackable,
      Sendable
    {
      /// Required. The Log Analytics SQL query to run, as a string.  The query
      /// must conform to the required shape. Specifically, the query must not
      /// try to filter the input by time.  A filter will automatically be
      /// applied to filter the input so that the query receives all rows
      /// received since the last time the query was run.
      ///
      /// For example, the following query extracts all log entries containing an
      /// HTTP request:
      ///
      ///     SELECT
      ///       timestamp, log_name, severity, http_request, resource, labels
      ///     FROM
      ///       my-project.global._Default._AllLogs
      ///     WHERE
      ///       http_request IS NOT NULL
      public var query: Swift.String = Swift.String()

      /// The schedule indicates how often the query should be run.
      public var schedule: OneOf_Schedule? = nil

      /// The test to be run against the SQL result set.
      public var evaluate: OneOf_Evaluate? = nil

      @_spi(GoogleCloudInternal) public var _unknownFields: GoogleWKT._UnknownFields = .init()

      /// Initialize a new instance of `SqlCondition`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = SqlCondition().with { $0.query = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      private struct CodingKeys: CodingKey {
        var stringValue: Swift.String
        var intValue: Swift.Int? { nil }
        init(stringValue: Swift.String) { self.stringValue = stringValue }
        init?(intValue: Swift.Int) { nil }

        static let query = CodingKeys(stringValue: "query")
        static let minutes = CodingKeys(stringValue: "minutes")
        static let hourly = CodingKeys(stringValue: "hourly")
        static let daily = CodingKeys(stringValue: "daily")
        static let rowCountTest = CodingKeys(stringValue: "rowCountTest")
        static let booleanTest = CodingKeys(stringValue: "booleanTest")

        static let _knownKeys: Set<Swift.String> = [
          "query",
          "minutes",
          "hourly",
          "daily",
          "rowCountTest",
          "booleanTest",
        ]
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        if let value = try container.decodeIfPresent(Swift.String.self, forKey: .query) {
          self.query = value
        }

        var schedule: OneOf_Schedule? = nil
        let scheduleCheckAndSet = {
          if schedule != nil {
            throw DecodingError.dataCorrupted(
              DecodingError.Context(
                codingPath: decoder.codingPath,
                debugDescription: "Multiple values set for oneof 'schedule'"))
          }
          schedule = $0
        }
        if let minutes = try container.decodeIfPresent(
          AlertPolicy.Condition.SqlCondition.Minutes?.self, forKey: .minutes)
        {
          try scheduleCheckAndSet(.minutes(minutes))
        }
        if let hourly = try container.decodeIfPresent(
          AlertPolicy.Condition.SqlCondition.Hourly?.self, forKey: .hourly)
        {
          try scheduleCheckAndSet(.hourly(hourly))
        }
        if let daily = try container.decodeIfPresent(
          AlertPolicy.Condition.SqlCondition.Daily?.self, forKey: .daily)
        {
          try scheduleCheckAndSet(.daily(daily))
        }
        self.schedule = schedule

        var evaluate: OneOf_Evaluate? = nil
        let evaluateCheckAndSet = {
          if evaluate != nil {
            throw DecodingError.dataCorrupted(
              DecodingError.Context(
                codingPath: decoder.codingPath,
                debugDescription: "Multiple values set for oneof 'evaluate'"))
          }
          evaluate = $0
        }
        if let rowCountTest = try container.decodeIfPresent(
          AlertPolicy.Condition.SqlCondition.RowCountTest?.self, forKey: .rowCountTest)
        {
          try evaluateCheckAndSet(.rowCountTest(rowCountTest))
        }
        if let booleanTest = try container.decodeIfPresent(
          AlertPolicy.Condition.SqlCondition.BooleanTest?.self, forKey: .booleanTest)
        {
          try evaluateCheckAndSet(.booleanTest(booleanTest))
        }
        self.evaluate = evaluate
        for key in container.allKeys where !CodingKeys._knownKeys.contains(key.stringValue) {
          self._unknownFields.json[key.stringValue] = try container.decode(
            GoogleWKT.Value.self, forKey: key)
        }
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.query, forKey: .query)

        if let choice = self.schedule {
          switch choice {
          case .minutes(let value):
            try container.encode(value, forKey: .minutes)
          case .hourly(let value):
            try container.encode(value, forKey: .hourly)
          case .daily(let value):
            try container.encode(value, forKey: .daily)
          }
        }

        if let choice = self.evaluate {
          switch choice {
          case .rowCountTest(let value):
            try container.encode(value, forKey: .rowCountTest)
          case .booleanTest(let value):
            try container.encode(value, forKey: .booleanTest)
          }
        }
        for (key, value) in self._unknownFields.json {
          try container.encode(value, forKey: CodingKeys(stringValue: key))
        }
      }

      /// Used to schedule the query to run every so many minutes.
      public struct Minutes: Codable, Equatable, GoogleWKT._AnyPackable,
        Sendable
      {
        /// Required. Number of minutes between runs. The interval must be
        /// greater than or equal to 5 minutes and less than or equal to 1440
        /// minutes.
        public var periodicity: Swift.Int32 = Swift.Int32()

        @_spi(GoogleCloudInternal) public var _unknownFields: GoogleWKT._UnknownFields = .init()

        /// Initialize a new instance of `Minutes`.
        public init() {}

        /// Use `config` to return a new instance of this object, with some fields updated.
        ///
        /// Commonly used to initialize the value, for example:
        ///
        /// ```
        /// let value = Minutes().with { $0.periodicity = ... }
        /// ```
        public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
          var copy = self
          try config(&copy)
          return copy
        }

        private struct CodingKeys: CodingKey {
          var stringValue: Swift.String
          var intValue: Swift.Int? { nil }
          init(stringValue: Swift.String) { self.stringValue = stringValue }
          init?(intValue: Swift.Int) { nil }

          static let periodicity = CodingKeys(stringValue: "periodicity")

          static let _knownKeys: Set<Swift.String> = [
            "periodicity"
          ]
        }

        public init(from decoder: Decoder) throws {
          let container = try decoder.container(keyedBy: CodingKeys.self)
          if let value = try container.decodeIfPresent(Swift.Int32.self, forKey: .periodicity) {
            self.periodicity = value
          }
          for key in container.allKeys where !CodingKeys._knownKeys.contains(key.stringValue) {
            self._unknownFields.json[key.stringValue] = try container.decode(
              GoogleWKT.Value.self, forKey: key)
          }
        }

        public func encode(to encoder: Encoder) throws {
          var container = encoder.container(keyedBy: CodingKeys.self)
          try container.encode(self.periodicity, forKey: .periodicity)
          for (key, value) in self._unknownFields.json {
            try container.encode(value, forKey: CodingKeys(stringValue: key))
          }
        }

        public static var _anyTypeUrl: Swift.String {
          return
            "type.googleapis.com/google.monitoring.v3.AlertPolicy.Condition.SqlCondition.Minutes"
        }
        public init(fromAny any: GoogleWKT.`Any`) throws {
          self = try GoogleWKT._slowAnyDeserialize(Self.self, from: any)
        }
        public func _pack() throws -> GoogleWKT.Struct {
          return try GoogleWKT._slowAnySerialize(message: self)
        }
      }

      /// Used to schedule the query to run every so many hours.
      public struct Hourly: Codable, Equatable, GoogleWKT._AnyPackable,
        Sendable
      {
        /// Required. The number of hours between runs. Must be greater than or
        /// equal to 1 hour and less than or equal to 48 hours.
        public var periodicity: Swift.Int32 = Swift.Int32()

        /// Optional. The number of minutes after the hour (in UTC) to run the
        /// query. Must be greater than or equal to 0 minutes and less than or
        /// equal to 59 minutes.  If left unspecified, then an arbitrary offset
        /// is used.
        public var minuteOffset: Swift.Int32? = nil

        @_spi(GoogleCloudInternal) public var _unknownFields: GoogleWKT._UnknownFields = .init()

        /// Initialize a new instance of `Hourly`.
        public init() {}

        /// Use `config` to return a new instance of this object, with some fields updated.
        ///
        /// Commonly used to initialize the value, for example:
        ///
        /// ```
        /// let value = Hourly().with { $0.periodicity = ... }
        /// ```
        public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
          var copy = self
          try config(&copy)
          return copy
        }

        private struct CodingKeys: CodingKey {
          var stringValue: Swift.String
          var intValue: Swift.Int? { nil }
          init(stringValue: Swift.String) { self.stringValue = stringValue }
          init?(intValue: Swift.Int) { nil }

          static let periodicity = CodingKeys(stringValue: "periodicity")
          static let minuteOffset = CodingKeys(stringValue: "minuteOffset")

          static let _knownKeys: Set<Swift.String> = [
            "periodicity",
            "minuteOffset",
          ]
        }

        public init(from decoder: Decoder) throws {
          let container = try decoder.container(keyedBy: CodingKeys.self)
          if let value = try container.decodeIfPresent(Swift.Int32.self, forKey: .periodicity) {
            self.periodicity = value
          }
          self.minuteOffset = try container.decodeIfPresent(Swift.Int32.self, forKey: .minuteOffset)
          for key in container.allKeys where !CodingKeys._knownKeys.contains(key.stringValue) {
            self._unknownFields.json[key.stringValue] = try container.decode(
              GoogleWKT.Value.self, forKey: key)
          }
        }

        public func encode(to encoder: Encoder) throws {
          var container = encoder.container(keyedBy: CodingKeys.self)
          try container.encode(self.periodicity, forKey: .periodicity)
          try container.encodeIfPresent(self.minuteOffset, forKey: .minuteOffset)
          for (key, value) in self._unknownFields.json {
            try container.encode(value, forKey: CodingKeys(stringValue: key))
          }
        }

        public static var _anyTypeUrl: Swift.String {
          return
            "type.googleapis.com/google.monitoring.v3.AlertPolicy.Condition.SqlCondition.Hourly"
        }
        public init(fromAny any: GoogleWKT.`Any`) throws {
          self = try GoogleWKT._slowAnyDeserialize(Self.self, from: any)
        }
        public func _pack() throws -> GoogleWKT.Struct {
          return try GoogleWKT._slowAnySerialize(message: self)
        }
      }

      /// Used to schedule the query to run every so many days.
      public struct Daily: Codable, Equatable, GoogleWKT._AnyPackable,
        Sendable
      {
        /// Required. The number of days between runs. Must be greater than or
        /// equal to 1 day and less than or equal to 31 days.
        public var periodicity: Swift.Int32 = Swift.Int32()

        /// Optional. The time of day (in UTC) at which the query should run. If
        /// left unspecified, the server picks an arbitrary time of day and runs
        /// the query at the same time each day.
        public var executionTime: GoogleType.TimeOfDay? = nil

        @_spi(GoogleCloudInternal) public var _unknownFields: GoogleWKT._UnknownFields = .init()

        /// Initialize a new instance of `Daily`.
        public init() {}

        /// Use `config` to return a new instance of this object, with some fields updated.
        ///
        /// Commonly used to initialize the value, for example:
        ///
        /// ```
        /// let value = Daily().with { $0.periodicity = ... }
        /// ```
        public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
          var copy = self
          try config(&copy)
          return copy
        }

        private struct CodingKeys: CodingKey {
          var stringValue: Swift.String
          var intValue: Swift.Int? { nil }
          init(stringValue: Swift.String) { self.stringValue = stringValue }
          init?(intValue: Swift.Int) { nil }

          static let periodicity = CodingKeys(stringValue: "periodicity")
          static let executionTime = CodingKeys(stringValue: "executionTime")

          static let _knownKeys: Set<Swift.String> = [
            "periodicity",
            "executionTime",
          ]
        }

        public init(from decoder: Decoder) throws {
          let container = try decoder.container(keyedBy: CodingKeys.self)
          if let value = try container.decodeIfPresent(Swift.Int32.self, forKey: .periodicity) {
            self.periodicity = value
          }
          self.executionTime = try container.decodeIfPresent(
            GoogleType.TimeOfDay.self, forKey: .executionTime)
          for key in container.allKeys where !CodingKeys._knownKeys.contains(key.stringValue) {
            self._unknownFields.json[key.stringValue] = try container.decode(
              GoogleWKT.Value.self, forKey: key)
          }
        }

        public func encode(to encoder: Encoder) throws {
          var container = encoder.container(keyedBy: CodingKeys.self)
          try container.encode(self.periodicity, forKey: .periodicity)
          try container.encodeIfPresent(self.executionTime, forKey: .executionTime)
          for (key, value) in self._unknownFields.json {
            try container.encode(value, forKey: CodingKeys(stringValue: key))
          }
        }

        public static var _anyTypeUrl: Swift.String {
          return "type.googleapis.com/google.monitoring.v3.AlertPolicy.Condition.SqlCondition.Daily"
        }
        public init(fromAny any: GoogleWKT.`Any`) throws {
          self = try GoogleWKT._slowAnyDeserialize(Self.self, from: any)
        }
        public func _pack() throws -> GoogleWKT.Struct {
          return try GoogleWKT._slowAnySerialize(message: self)
        }
      }

      /// A test that checks if the number of rows in the result set
      /// violates some threshold.
      public struct RowCountTest: Codable, Equatable, GoogleWKT._AnyPackable,
        Sendable
      {
        /// Required. The comparison to apply between the number of rows returned
        /// by the query and the threshold.
        public var comparison: ComparisonType = ComparisonType()

        /// Required. The value against which to compare the row count.
        public var threshold: Swift.Int64 = Swift.Int64()

        @_spi(GoogleCloudInternal) public var _unknownFields: GoogleWKT._UnknownFields = .init()

        /// Initialize a new instance of `RowCountTest`.
        public init() {}

        /// Use `config` to return a new instance of this object, with some fields updated.
        ///
        /// Commonly used to initialize the value, for example:
        ///
        /// ```
        /// let value = RowCountTest().with { $0.comparison = ... }
        /// ```
        public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
          var copy = self
          try config(&copy)
          return copy
        }

        private struct CodingKeys: CodingKey {
          var stringValue: Swift.String
          var intValue: Swift.Int? { nil }
          init(stringValue: Swift.String) { self.stringValue = stringValue }
          init?(intValue: Swift.Int) { nil }

          static let comparison = CodingKeys(stringValue: "comparison")
          static let threshold = CodingKeys(stringValue: "threshold")

          static let _knownKeys: Set<Swift.String> = [
            "comparison",
            "threshold",
          ]
        }

        public init(from decoder: Decoder) throws {
          let container = try decoder.container(keyedBy: CodingKeys.self)
          if let value = try container.decodeIfPresent(ComparisonType.self, forKey: .comparison) {
            self.comparison = value
          }
          if let value = try container.decodeIfPresent(Swift.Int64.self, forKey: .threshold) {
            self.threshold = value
          }
          for key in container.allKeys where !CodingKeys._knownKeys.contains(key.stringValue) {
            self._unknownFields.json[key.stringValue] = try container.decode(
              GoogleWKT.Value.self, forKey: key)
          }
        }

        public func encode(to encoder: Encoder) throws {
          var container = encoder.container(keyedBy: CodingKeys.self)
          try container.encode(self.comparison, forKey: .comparison)
          try container.encode(self.threshold, forKey: .threshold)
          for (key, value) in self._unknownFields.json {
            try container.encode(value, forKey: CodingKeys(stringValue: key))
          }
        }

        public static var _anyTypeUrl: Swift.String {
          return
            "type.googleapis.com/google.monitoring.v3.AlertPolicy.Condition.SqlCondition.RowCountTest"
        }
        public init(fromAny any: GoogleWKT.`Any`) throws {
          self = try GoogleWKT._slowAnyDeserialize(Self.self, from: any)
        }
        public func _pack() throws -> GoogleWKT.Struct {
          return try GoogleWKT._slowAnySerialize(message: self)
        }
      }

      /// A test that uses an alerting result in a boolean column produced by
      /// the SQL query.
      public struct BooleanTest: Codable, Equatable, GoogleWKT._AnyPackable,
        Sendable
      {
        /// Required. The name of the column containing the boolean value. If the
        /// value in a row is NULL, that row is ignored.
        public var column: Swift.String = Swift.String()

        @_spi(GoogleCloudInternal) public var _unknownFields: GoogleWKT._UnknownFields = .init()

        /// Initialize a new instance of `BooleanTest`.
        public init() {}

        /// Use `config` to return a new instance of this object, with some fields updated.
        ///
        /// Commonly used to initialize the value, for example:
        ///
        /// ```
        /// let value = BooleanTest().with { $0.column = ... }
        /// ```
        public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
          var copy = self
          try config(&copy)
          return copy
        }

        private struct CodingKeys: CodingKey {
          var stringValue: Swift.String
          var intValue: Swift.Int? { nil }
          init(stringValue: Swift.String) { self.stringValue = stringValue }
          init?(intValue: Swift.Int) { nil }

          static let column = CodingKeys(stringValue: "column")

          static let _knownKeys: Set<Swift.String> = [
            "column"
          ]
        }

        public init(from decoder: Decoder) throws {
          let container = try decoder.container(keyedBy: CodingKeys.self)
          if let value = try container.decodeIfPresent(Swift.String.self, forKey: .column) {
            self.column = value
          }
          for key in container.allKeys where !CodingKeys._knownKeys.contains(key.stringValue) {
            self._unknownFields.json[key.stringValue] = try container.decode(
              GoogleWKT.Value.self, forKey: key)
          }
        }

        public func encode(to encoder: Encoder) throws {
          var container = encoder.container(keyedBy: CodingKeys.self)
          try container.encode(self.column, forKey: .column)
          for (key, value) in self._unknownFields.json {
            try container.encode(value, forKey: CodingKeys(stringValue: key))
          }
        }

        public static var _anyTypeUrl: Swift.String {
          return
            "type.googleapis.com/google.monitoring.v3.AlertPolicy.Condition.SqlCondition.BooleanTest"
        }
        public init(fromAny any: GoogleWKT.`Any`) throws {
          self = try GoogleWKT._slowAnyDeserialize(Self.self, from: any)
        }
        public func _pack() throws -> GoogleWKT.Struct {
          return try GoogleWKT._slowAnySerialize(message: self)
        }
      }

      /// The schedule indicates how often the query should be run.
      public enum OneOf_Schedule: Codable, Equatable, Sendable {
        /// Schedule the query to execute every so many minutes.
        indirect case minutes(AlertPolicy.Condition.SqlCondition.Minutes?)
        /// Schedule the query to execute every so many hours.
        indirect case hourly(AlertPolicy.Condition.SqlCondition.Hourly?)
        /// Schedule the query to execute every so many days.
        indirect case daily(AlertPolicy.Condition.SqlCondition.Daily?)
      }

      /// The test to be run against the SQL result set.
      public enum OneOf_Evaluate: Codable, Equatable, Sendable {
        /// Test the row count against a threshold.
        indirect case rowCountTest(AlertPolicy.Condition.SqlCondition.RowCountTest?)
        /// Test the boolean value in the indicated column.
        indirect case booleanTest(AlertPolicy.Condition.SqlCondition.BooleanTest?)
      }

      public static var _anyTypeUrl: Swift.String {
        return "type.googleapis.com/google.monitoring.v3.AlertPolicy.Condition.SqlCondition"
      }
      public init(fromAny any: GoogleWKT.`Any`) throws {
        self = try GoogleWKT._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleWKT.Struct {
        return try GoogleWKT._slowAnySerialize(message: self)
      }
    }

    /// A condition control that determines how metric-threshold conditions
    /// are evaluated when data stops arriving.
    /// This control doesn't affect metric-absence policies.
    public enum EvaluationMissingData: Codable, Equatable, Sendable {
      /// An unspecified evaluation missing data option.  Equivalent to
      /// EVALUATION_MISSING_DATA_NO_OP.
      case unspecified
      /// If there is no data to evaluate the condition, then evaluate the
      /// condition as false.
      case inactive
      /// If there is no data to evaluate the condition, then evaluate the
      /// condition as true.
      case active
      /// Do not evaluate the condition to any value if there is no data.
      case noOp
      /// Encodes an unknown integer value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownIntValue(Int)
      /// Encodes an unknown string value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownStringValue(String)

      public init() {
        self = .unspecified
      }

      /// Returns the integer value associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown string value, this returns `nil`.
      public var intValue: Int? {
        switch self {
        case .unspecified: return 0
        case .inactive: return 1
        case .active: return 2
        case .noOp: return 3
        case .unknownIntValue(let v): return v
        case .unknownStringValue: return nil
        }
      }

      /// Returns the string value (or name) associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
      public var stringValue: Swift.String? {
        switch self {
        case .unspecified: return "EVALUATION_MISSING_DATA_UNSPECIFIED"
        case .inactive: return "EVALUATION_MISSING_DATA_INACTIVE"
        case .active: return "EVALUATION_MISSING_DATA_ACTIVE"
        case .noOp: return "EVALUATION_MISSING_DATA_NO_OP"
        case .unknownIntValue: return nil
        case .unknownStringValue(let v): return v
        }
      }

      /// Initialize from a string value.
      ///
      /// If the value is unknown, this initializes to [`unknownStringValue`](doc:EvaluationMissingData/unknownStringValue(_:)).
      public init(stringValue: Swift.String) {
        switch stringValue {
        case "EVALUATION_MISSING_DATA_UNSPECIFIED": self = .unspecified
        case "EVALUATION_MISSING_DATA_INACTIVE": self = .inactive
        case "EVALUATION_MISSING_DATA_ACTIVE": self = .active
        case "EVALUATION_MISSING_DATA_NO_OP": self = .noOp
        default: self = .unknownStringValue(stringValue)
        }
      }

      /// Initialize from an integer value.
      ///
      /// If the value is unknown, this initializes to [`unknownIntValue`](doc:EvaluationMissingData/unknownIntValue(_:)).
      public init(intValue: Int) {
        switch intValue {
        case 0: self = .unspecified
        case 1: self = .inactive
        case 2: self = .active
        case 3: self = .noOp
        default: self = .unknownIntValue(intValue)
        }
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let v = try? container.decode(Int.self) {
          self.init(intValue: v)
          return
        }
        if let s = try? container.decode(String.self) {
          if let v = Int(s) {
            self.init(intValue: v)
          } else {
            self.init(stringValue: s)
          }
          return
        }
        throw DecodingError.dataCorruptedError(
          in: container, debugDescription: "Expected enum value, must be integer or string.")
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.singleValueContainer()
        switch self {
        case .unspecified: return try container.encode("EVALUATION_MISSING_DATA_UNSPECIFIED")
        case .inactive: return try container.encode("EVALUATION_MISSING_DATA_INACTIVE")
        case .active: return try container.encode("EVALUATION_MISSING_DATA_ACTIVE")
        case .noOp: return try container.encode("EVALUATION_MISSING_DATA_NO_OP")
        case .unknownIntValue(let v): return try container.encode(v)
        case .unknownStringValue(let v): return try container.encode(v)
        }
      }
    }

    /// Only one of the following condition types will be specified.
    public enum OneOf_Condition: Codable, Equatable, Sendable {
      /// A condition that compares a time series against a threshold.
      indirect case conditionThreshold(AlertPolicy.Condition.MetricThreshold?)
      /// A condition that checks that a time series continues to
      /// receive new data points.
      indirect case conditionAbsent(AlertPolicy.Condition.MetricAbsence?)
      /// A condition that checks for log messages matching given constraints. If
      /// set, no other conditions can be present.
      indirect case conditionMatchedLog(AlertPolicy.Condition.LogMatch?)
      /// A condition that uses the Monitoring Query Language to define
      /// alerts.
      indirect case conditionMonitoringQueryLanguage(
        AlertPolicy.Condition.MonitoringQueryLanguageCondition?)
      /// A condition that uses the Prometheus query language to define alerts.
      indirect case conditionPrometheusQueryLanguage(
        AlertPolicy.Condition.PrometheusQueryLanguageCondition?)
      /// A condition that periodically evaluates a SQL query result.
      indirect case conditionSql(AlertPolicy.Condition.SqlCondition?)
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.monitoring.v3.AlertPolicy.Condition"
    }
    public init(fromAny any: GoogleWKT.`Any`) throws {
      self = try GoogleWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleWKT.Struct {
      return try GoogleWKT._slowAnySerialize(message: self)
    }
  }

  /// Control over how the notification channels in `notification_channels`
  /// are notified when this alert fires.
  public struct AlertStrategy: Codable, Equatable, GoogleWKT._AnyPackable,
    Sendable
  {
    /// Required for log-based alerting policies, i.e. policies with a `LogMatch`
    /// condition.
    ///
    /// This limit is not implemented for alerting policies that do not have
    /// a LogMatch condition.
    public var notificationRateLimit: AlertPolicy.AlertStrategy.NotificationRateLimit? = nil

    /// For log-based alert policies, the notification prompts is always
    /// [OPENED]. For non log-based alert policies, the notification prompts can
    /// be [OPENED] or [OPENED, CLOSED].
    public var notificationPrompts: [AlertPolicy.AlertStrategy.NotificationPrompt] = []

    /// If an alerting policy that was active has no data for this long, any open
    /// incidents will close
    public var autoClose: GoogleWKT.Duration? = nil

    /// Control how notifications will be sent out, on a per-channel basis.
    public var notificationChannelStrategy:
      [AlertPolicy.AlertStrategy.NotificationChannelStrategy] = []

    @_spi(GoogleCloudInternal) public var _unknownFields: GoogleWKT._UnknownFields = .init()

    /// Initialize a new instance of `AlertStrategy`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = AlertStrategy().with { $0.notificationRateLimit = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    private struct CodingKeys: CodingKey {
      var stringValue: Swift.String
      var intValue: Swift.Int? { nil }
      init(stringValue: Swift.String) { self.stringValue = stringValue }
      init?(intValue: Swift.Int) { nil }

      static let notificationRateLimit = CodingKeys(stringValue: "notificationRateLimit")
      static let notificationPrompts = CodingKeys(stringValue: "notificationPrompts")
      static let autoClose = CodingKeys(stringValue: "autoClose")
      static let notificationChannelStrategy = CodingKeys(
        stringValue: "notificationChannelStrategy")

      static let _knownKeys: Set<Swift.String> = [
        "notificationRateLimit",
        "notificationPrompts",
        "autoClose",
        "notificationChannelStrategy",
      ]
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.container(keyedBy: CodingKeys.self)
      self.notificationRateLimit = try container.decodeIfPresent(
        AlertPolicy.AlertStrategy.NotificationRateLimit.self, forKey: .notificationRateLimit)
      if let value = try container.decodeIfPresent(
        [AlertPolicy.AlertStrategy.NotificationPrompt].self, forKey: .notificationPrompts)
      {
        self.notificationPrompts = value
      }
      self.autoClose = try container.decodeIfPresent(GoogleWKT.Duration.self, forKey: .autoClose)
      if let value = try container.decodeIfPresent(
        [AlertPolicy.AlertStrategy.NotificationChannelStrategy].self,
        forKey: .notificationChannelStrategy)
      {
        self.notificationChannelStrategy = value
      }
      for key in container.allKeys where !CodingKeys._knownKeys.contains(key.stringValue) {
        self._unknownFields.json[key.stringValue] = try container.decode(
          GoogleWKT.Value.self, forKey: key)
      }
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.container(keyedBy: CodingKeys.self)
      try container.encodeIfPresent(self.notificationRateLimit, forKey: .notificationRateLimit)
      try container.encode(self.notificationPrompts, forKey: .notificationPrompts)
      try container.encodeIfPresent(self.autoClose, forKey: .autoClose)
      try container.encode(self.notificationChannelStrategy, forKey: .notificationChannelStrategy)
      for (key, value) in self._unknownFields.json {
        try container.encode(value, forKey: CodingKeys(stringValue: key))
      }
    }

    /// Control over the rate of notifications sent to this alerting policy's
    /// notification channels.
    public struct NotificationRateLimit: Codable, Equatable, GoogleWKT._AnyPackable,
      Sendable
    {
      /// Not more than one notification per `period`.
      public var period: GoogleWKT.Duration? = nil

      @_spi(GoogleCloudInternal) public var _unknownFields: GoogleWKT._UnknownFields = .init()

      /// Initialize a new instance of `NotificationRateLimit`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = NotificationRateLimit().with { $0.period = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      private struct CodingKeys: CodingKey {
        var stringValue: Swift.String
        var intValue: Swift.Int? { nil }
        init(stringValue: Swift.String) { self.stringValue = stringValue }
        init?(intValue: Swift.Int) { nil }

        static let period = CodingKeys(stringValue: "period")

        static let _knownKeys: Set<Swift.String> = [
          "period"
        ]
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.period = try container.decodeIfPresent(GoogleWKT.Duration.self, forKey: .period)
        for key in container.allKeys where !CodingKeys._knownKeys.contains(key.stringValue) {
          self._unknownFields.json[key.stringValue] = try container.decode(
            GoogleWKT.Value.self, forKey: key)
        }
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(self.period, forKey: .period)
        for (key, value) in self._unknownFields.json {
          try container.encode(value, forKey: CodingKeys(stringValue: key))
        }
      }

      public static var _anyTypeUrl: Swift.String {
        return
          "type.googleapis.com/google.monitoring.v3.AlertPolicy.AlertStrategy.NotificationRateLimit"
      }
      public init(fromAny any: GoogleWKT.`Any`) throws {
        self = try GoogleWKT._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleWKT.Struct {
        return try GoogleWKT._slowAnySerialize(message: self)
      }
    }

    /// Control over how the notification channels in `notification_channels`
    /// are notified when this alert fires, on a per-channel basis.
    public struct NotificationChannelStrategy: Codable, Equatable, GoogleWKT._AnyPackable,
      Sendable
    {
      /// The full REST resource name for the notification channels that these
      /// settings apply to. Each of these correspond to the name field in one
      /// of the NotificationChannel objects referenced in the
      /// notification_channels field of this AlertPolicy.
      /// The format is:
      ///
      ///     projects/[PROJECT_ID_OR_NUMBER]/notificationChannels/[CHANNEL_ID]
      public var notificationChannelNames: [Swift.String] = []

      /// The frequency at which to send reminder notifications for open
      /// incidents.
      public var renotifyInterval: GoogleWKT.Duration? = nil

      @_spi(GoogleCloudInternal) public var _unknownFields: GoogleWKT._UnknownFields = .init()

      /// Initialize a new instance of `NotificationChannelStrategy`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = NotificationChannelStrategy().with { $0.notificationChannelNames = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      private struct CodingKeys: CodingKey {
        var stringValue: Swift.String
        var intValue: Swift.Int? { nil }
        init(stringValue: Swift.String) { self.stringValue = stringValue }
        init?(intValue: Swift.Int) { nil }

        static let notificationChannelNames = CodingKeys(stringValue: "notificationChannelNames")
        static let renotifyInterval = CodingKeys(stringValue: "renotifyInterval")

        static let _knownKeys: Set<Swift.String> = [
          "notificationChannelNames",
          "renotifyInterval",
        ]
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        if let value = try container.decodeIfPresent(
          [Swift.String].self, forKey: .notificationChannelNames)
        {
          self.notificationChannelNames = value
        }
        self.renotifyInterval = try container.decodeIfPresent(
          GoogleWKT.Duration.self, forKey: .renotifyInterval)
        for key in container.allKeys where !CodingKeys._knownKeys.contains(key.stringValue) {
          self._unknownFields.json[key.stringValue] = try container.decode(
            GoogleWKT.Value.self, forKey: key)
        }
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.notificationChannelNames, forKey: .notificationChannelNames)
        try container.encodeIfPresent(self.renotifyInterval, forKey: .renotifyInterval)
        for (key, value) in self._unknownFields.json {
          try container.encode(value, forKey: CodingKeys(stringValue: key))
        }
      }

      public static var _anyTypeUrl: Swift.String {
        return
          "type.googleapis.com/google.monitoring.v3.AlertPolicy.AlertStrategy.NotificationChannelStrategy"
      }
      public init(fromAny any: GoogleWKT.`Any`) throws {
        self = try GoogleWKT._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleWKT.Struct {
        return try GoogleWKT._slowAnySerialize(message: self)
      }
    }

    /// Control when notifications will be sent out.
    public enum NotificationPrompt: Codable, Equatable, Sendable {
      /// No strategy specified. Treated as error.
      case unspecified
      /// Notify when an incident is opened.
      case opened
      /// Notify when an incident is closed.
      case closed
      /// Encodes an unknown integer value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownIntValue(Int)
      /// Encodes an unknown string value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownStringValue(String)

      public init() {
        self = .unspecified
      }

      /// Returns the integer value associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown string value, this returns `nil`.
      public var intValue: Int? {
        switch self {
        case .unspecified: return 0
        case .opened: return 1
        case .closed: return 3
        case .unknownIntValue(let v): return v
        case .unknownStringValue: return nil
        }
      }

      /// Returns the string value (or name) associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
      public var stringValue: Swift.String? {
        switch self {
        case .unspecified: return "NOTIFICATION_PROMPT_UNSPECIFIED"
        case .opened: return "OPENED"
        case .closed: return "CLOSED"
        case .unknownIntValue: return nil
        case .unknownStringValue(let v): return v
        }
      }

      /// Initialize from a string value.
      ///
      /// If the value is unknown, this initializes to [`unknownStringValue`](doc:NotificationPrompt/unknownStringValue(_:)).
      public init(stringValue: Swift.String) {
        switch stringValue {
        case "NOTIFICATION_PROMPT_UNSPECIFIED": self = .unspecified
        case "OPENED": self = .opened
        case "CLOSED": self = .closed
        default: self = .unknownStringValue(stringValue)
        }
      }

      /// Initialize from an integer value.
      ///
      /// If the value is unknown, this initializes to [`unknownIntValue`](doc:NotificationPrompt/unknownIntValue(_:)).
      public init(intValue: Int) {
        switch intValue {
        case 0: self = .unspecified
        case 1: self = .opened
        case 3: self = .closed
        default: self = .unknownIntValue(intValue)
        }
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let v = try? container.decode(Int.self) {
          self.init(intValue: v)
          return
        }
        if let s = try? container.decode(String.self) {
          if let v = Int(s) {
            self.init(intValue: v)
          } else {
            self.init(stringValue: s)
          }
          return
        }
        throw DecodingError.dataCorruptedError(
          in: container, debugDescription: "Expected enum value, must be integer or string.")
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.singleValueContainer()
        switch self {
        case .unspecified: return try container.encode("NOTIFICATION_PROMPT_UNSPECIFIED")
        case .opened: return try container.encode("OPENED")
        case .closed: return try container.encode("CLOSED")
        case .unknownIntValue(let v): return try container.encode(v)
        case .unknownStringValue(let v): return try container.encode(v)
        }
      }
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.monitoring.v3.AlertPolicy.AlertStrategy"
    }
    public init(fromAny any: GoogleWKT.`Any`) throws {
      self = try GoogleWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleWKT.Struct {
      return try GoogleWKT._slowAnySerialize(message: self)
    }
  }

  /// Operators for combining conditions.
  public enum ConditionCombinerType: Codable, Equatable, Sendable {
    /// An unspecified combiner.
    case combineUnspecified
    /// Combine conditions using the logical `AND` operator. An
    /// incident is created only if all the conditions are met
    /// simultaneously. This combiner is satisfied if all conditions are
    /// met, even if they are met on completely different resources.
    case and
    /// Combine conditions using the logical `OR` operator. An incident
    /// is created if any of the listed conditions is met.
    case or
    /// Combine conditions using logical `AND` operator, but unlike the regular
    /// `AND` option, an incident is created only if all conditions are met
    /// simultaneously on at least one resource.
    case andWithMatchingResource
    /// Encodes an unknown integer value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownIntValue(Int)
    /// Encodes an unknown string value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownStringValue(String)

    public init() {
      self = .combineUnspecified
    }

    /// Returns the integer value associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown string value, this returns `nil`.
    public var intValue: Int? {
      switch self {
      case .combineUnspecified: return 0
      case .and: return 1
      case .or: return 2
      case .andWithMatchingResource: return 3
      case .unknownIntValue(let v): return v
      case .unknownStringValue: return nil
      }
    }

    /// Returns the string value (or name) associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
    public var stringValue: Swift.String? {
      switch self {
      case .combineUnspecified: return "COMBINE_UNSPECIFIED"
      case .and: return "AND"
      case .or: return "OR"
      case .andWithMatchingResource: return "AND_WITH_MATCHING_RESOURCE"
      case .unknownIntValue: return nil
      case .unknownStringValue(let v): return v
      }
    }

    /// Initialize from a string value.
    ///
    /// If the value is unknown, this initializes to [`unknownStringValue`](doc:ConditionCombinerType/unknownStringValue(_:)).
    public init(stringValue: Swift.String) {
      switch stringValue {
      case "COMBINE_UNSPECIFIED": self = .combineUnspecified
      case "AND": self = .and
      case "OR": self = .or
      case "AND_WITH_MATCHING_RESOURCE": self = .andWithMatchingResource
      default: self = .unknownStringValue(stringValue)
      }
    }

    /// Initialize from an integer value.
    ///
    /// If the value is unknown, this initializes to [`unknownIntValue`](doc:ConditionCombinerType/unknownIntValue(_:)).
    public init(intValue: Int) {
      switch intValue {
      case 0: self = .combineUnspecified
      case 1: self = .and
      case 2: self = .or
      case 3: self = .andWithMatchingResource
      default: self = .unknownIntValue(intValue)
      }
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.singleValueContainer()
      if let v = try? container.decode(Int.self) {
        self.init(intValue: v)
        return
      }
      if let s = try? container.decode(String.self) {
        if let v = Int(s) {
          self.init(intValue: v)
        } else {
          self.init(stringValue: s)
        }
        return
      }
      throw DecodingError.dataCorruptedError(
        in: container, debugDescription: "Expected enum value, must be integer or string.")
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.singleValueContainer()
      switch self {
      case .combineUnspecified: return try container.encode("COMBINE_UNSPECIFIED")
      case .and: return try container.encode("AND")
      case .or: return try container.encode("OR")
      case .andWithMatchingResource: return try container.encode("AND_WITH_MATCHING_RESOURCE")
      case .unknownIntValue(let v): return try container.encode(v)
      case .unknownStringValue(let v): return try container.encode(v)
      }
    }
  }

  /// An enumeration of possible severity level for an alerting policy.
  public enum Severity: Codable, Equatable, Sendable {
    /// No severity is specified. This is the default value.
    case unspecified
    /// This is the highest severity level. Use this if the problem could
    /// cause significant damage or downtime.
    case critical
    /// This is the medium severity level. Use this if the problem could
    /// cause minor damage or downtime.
    case error
    /// This is the lowest severity level. Use this if the problem is not causing
    /// any damage or downtime, but could potentially lead to a problem in the
    /// future.
    case warning
    /// Encodes an unknown integer value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownIntValue(Int)
    /// Encodes an unknown string value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownStringValue(String)

    public init() {
      self = .unspecified
    }

    /// Returns the integer value associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown string value, this returns `nil`.
    public var intValue: Int? {
      switch self {
      case .unspecified: return 0
      case .critical: return 1
      case .error: return 2
      case .warning: return 3
      case .unknownIntValue(let v): return v
      case .unknownStringValue: return nil
      }
    }

    /// Returns the string value (or name) associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
    public var stringValue: Swift.String? {
      switch self {
      case .unspecified: return "SEVERITY_UNSPECIFIED"
      case .critical: return "CRITICAL"
      case .error: return "ERROR"
      case .warning: return "WARNING"
      case .unknownIntValue: return nil
      case .unknownStringValue(let v): return v
      }
    }

    /// Initialize from a string value.
    ///
    /// If the value is unknown, this initializes to [`unknownStringValue`](doc:Severity/unknownStringValue(_:)).
    public init(stringValue: Swift.String) {
      switch stringValue {
      case "SEVERITY_UNSPECIFIED": self = .unspecified
      case "CRITICAL": self = .critical
      case "ERROR": self = .error
      case "WARNING": self = .warning
      default: self = .unknownStringValue(stringValue)
      }
    }

    /// Initialize from an integer value.
    ///
    /// If the value is unknown, this initializes to [`unknownIntValue`](doc:Severity/unknownIntValue(_:)).
    public init(intValue: Int) {
      switch intValue {
      case 0: self = .unspecified
      case 1: self = .critical
      case 2: self = .error
      case 3: self = .warning
      default: self = .unknownIntValue(intValue)
      }
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.singleValueContainer()
      if let v = try? container.decode(Int.self) {
        self.init(intValue: v)
        return
      }
      if let s = try? container.decode(String.self) {
        if let v = Int(s) {
          self.init(intValue: v)
        } else {
          self.init(stringValue: s)
        }
        return
      }
      throw DecodingError.dataCorruptedError(
        in: container, debugDescription: "Expected enum value, must be integer or string.")
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.singleValueContainer()
      switch self {
      case .unspecified: return try container.encode("SEVERITY_UNSPECIFIED")
      case .critical: return try container.encode("CRITICAL")
      case .error: return try container.encode("ERROR")
      case .warning: return try container.encode("WARNING")
      case .unknownIntValue(let v): return try container.encode(v)
      case .unknownStringValue(let v): return try container.encode(v)
      }
    }
  }

  public static var _anyTypeUrl: Swift.String {
    return "type.googleapis.com/google.monitoring.v3.AlertPolicy"
  }
  public init(fromAny any: GoogleWKT.`Any`) throws {
    self = try GoogleWKT._slowAnyDeserialize(Self.self, from: any)
  }
  public func _pack() throws -> GoogleWKT.Struct {
    return try GoogleWKT._slowAnySerialize(message: self)
  }
}
