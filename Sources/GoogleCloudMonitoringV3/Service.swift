// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

import Foundation
@_spi(GoogleCloudInternal) import GoogleCloudWKT

/// A `Service` is a discrete, autonomous, and network-accessible unit, designed
/// to solve an individual concern
/// ([Wikipedia](https://en.wikipedia.org/wiki/Service-orientation)). In
/// Cloud Monitoring, a `Service` acts as the root resource under which
/// operational aspects of the service are accessible.
public struct Service: Codable, Equatable, GoogleCloudWKT._AnyPackable,
  Sendable
{
  /// Identifier. Resource name for this Service. The format is:
  ///
  ///     projects/[PROJECT_ID_OR_NUMBER]/services/[SERVICE_ID]
  public var name: Swift.String = Swift.String()

  /// Name used for UI elements listing this Service.
  public var displayName: Swift.String = Swift.String()

  /// Message that contains the service type and service labels of this service
  /// if it is a basic service.
  /// Documentation and examples
  /// [here](https://cloud.google.com/stackdriver/docs/solutions/slo-monitoring/api/api-structures#basic-svc-w-basic-sli).
  public var basicService: Service.BasicService? = nil

  /// Configuration for how to query telemetry on a Service.
  public var telemetry: Service.Telemetry? = nil

  /// Labels which have been used to annotate the service. Label keys must start
  /// with a letter. Label keys and values may contain lowercase letters,
  /// numbers, underscores, and dashes. Label keys and values have a maximum
  /// length of 63 characters, and must be less than 128 bytes in size. Up to 64
  /// label entries may be stored. For labels which do not have a semantic value,
  /// the empty string may be supplied for the label value.
  public var userLabels: [Swift.String: Swift.String] = [:]

  /// REQUIRED. Service-identifying atoms specifying the underlying service.
  public var identifier: OneOf_Identifier? = nil

  /// Initialize a new instance of `Service`.
  public init() {}

  /// Use `config` to return a new instance of this object, with some fields updated.
  ///
  /// Commonly used to initialize the value, for example:
  ///
  /// ```
  /// let value = Service().with { $0.name = ... }
  /// ```
  public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
    var copy = self
    try config(&copy)
    return copy
  }

  private enum CodingKeys: Swift.String, CodingKey {
    case name = "name"
    case displayName = "displayName"
    case custom = "custom"
    case appEngine = "appEngine"
    case cloudEndpoints = "cloudEndpoints"
    case clusterIstio = "clusterIstio"
    case meshIstio = "meshIstio"
    case istioCanonicalService = "istioCanonicalService"
    case cloudRun = "cloudRun"
    case gkeNamespace = "gkeNamespace"
    case gkeWorkload = "gkeWorkload"
    case gkeService = "gkeService"
    case basicService = "basicService"
    case telemetry = "telemetry"
    case userLabels = "userLabels"
  }

  public init(from decoder: Decoder) throws {
    let container = try decoder.container(keyedBy: CodingKeys.self)
    self.name = try container.decode(Swift.String.self, forKey: .name)
    self.displayName = try container.decode(Swift.String.self, forKey: .displayName)
    self.basicService = try container.decodeIfPresent(
      Service.BasicService.self, forKey: .basicService)
    self.telemetry = try container.decodeIfPresent(Service.Telemetry.self, forKey: .telemetry)
    self.userLabels = try container.decode([Swift.String: Swift.String].self, forKey: .userLabels)

    var identifier: OneOf_Identifier? = nil
    let identifierCheckAndSet = {
      if identifier != nil {
        throw DecodingError.dataCorrupted(
          DecodingError.Context(
            codingPath: decoder.codingPath,
            debugDescription: "Multiple values set for oneof 'identifier'"))
      }
      identifier = $0
    }
    if let custom = try container.decodeIfPresent(Service.Custom?.self, forKey: .custom) {
      try identifierCheckAndSet(.custom(custom))
    }
    if let appEngine = try container.decodeIfPresent(Service.AppEngine?.self, forKey: .appEngine) {
      try identifierCheckAndSet(.appEngine(appEngine))
    }
    if let cloudEndpoints = try container.decodeIfPresent(
      Service.CloudEndpoints?.self, forKey: .cloudEndpoints)
    {
      try identifierCheckAndSet(.cloudEndpoints(cloudEndpoints))
    }
    if let clusterIstio = try container.decodeIfPresent(
      Service.ClusterIstio?.self, forKey: .clusterIstio)
    {
      try identifierCheckAndSet(.clusterIstio(clusterIstio))
    }
    if let meshIstio = try container.decodeIfPresent(Service.MeshIstio?.self, forKey: .meshIstio) {
      try identifierCheckAndSet(.meshIstio(meshIstio))
    }
    if let istioCanonicalService = try container.decodeIfPresent(
      Service.IstioCanonicalService?.self, forKey: .istioCanonicalService)
    {
      try identifierCheckAndSet(.istioCanonicalService(istioCanonicalService))
    }
    if let cloudRun = try container.decodeIfPresent(Service.CloudRun?.self, forKey: .cloudRun) {
      try identifierCheckAndSet(.cloudRun(cloudRun))
    }
    if let gkeNamespace = try container.decodeIfPresent(
      Service.GkeNamespace?.self, forKey: .gkeNamespace)
    {
      try identifierCheckAndSet(.gkeNamespace(gkeNamespace))
    }
    if let gkeWorkload = try container.decodeIfPresent(
      Service.GkeWorkload?.self, forKey: .gkeWorkload)
    {
      try identifierCheckAndSet(.gkeWorkload(gkeWorkload))
    }
    if let gkeService = try container.decodeIfPresent(Service.GkeService?.self, forKey: .gkeService)
    {
      try identifierCheckAndSet(.gkeService(gkeService))
    }
    self.identifier = identifier
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.container(keyedBy: CodingKeys.self)
    try container.encode(self.name, forKey: .name)
    try container.encode(self.displayName, forKey: .displayName)
    try container.encode(self.basicService, forKey: .basicService)
    try container.encode(self.telemetry, forKey: .telemetry)
    try container.encode(self.userLabels, forKey: .userLabels)

    if let choice = self.identifier {
      switch choice {
      case .custom(let value):
        try container.encode(value, forKey: .custom)
      case .appEngine(let value):
        try container.encode(value, forKey: .appEngine)
      case .cloudEndpoints(let value):
        try container.encode(value, forKey: .cloudEndpoints)
      case .clusterIstio(let value):
        try container.encode(value, forKey: .clusterIstio)
      case .meshIstio(let value):
        try container.encode(value, forKey: .meshIstio)
      case .istioCanonicalService(let value):
        try container.encode(value, forKey: .istioCanonicalService)
      case .cloudRun(let value):
        try container.encode(value, forKey: .cloudRun)
      case .gkeNamespace(let value):
        try container.encode(value, forKey: .gkeNamespace)
      case .gkeWorkload(let value):
        try container.encode(value, forKey: .gkeWorkload)
      case .gkeService(let value):
        try container.encode(value, forKey: .gkeService)
      }
    }
  }

  /// Use a custom service to designate a service that you want to monitor
  /// when none of the other service types (like App Engine, Cloud Run, or
  /// a GKE type) matches your intended service.
  public struct Custom: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// Initialize a new instance of `Custom`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = Custom().with { $0.<placeholder> = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.monitoring.v3.Service.Custom"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  /// App Engine service. Learn more at https://cloud.google.com/appengine.
  public struct AppEngine: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// The ID of the App Engine module underlying this service. Corresponds to
    /// the `module_id` resource label in the [`gae_app` monitored
    /// resource](https://cloud.google.com/monitoring/api/resources#tag_gae_app).
    public var moduleId: Swift.String = Swift.String()

    /// Initialize a new instance of `AppEngine`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = AppEngine().with { $0.moduleId = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.monitoring.v3.Service.AppEngine"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  /// Cloud Endpoints service. Learn more at https://cloud.google.com/endpoints.
  public struct CloudEndpoints: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// The name of the Cloud Endpoints service underlying this service.
    /// Corresponds to the `service` resource label in the [`api` monitored
    /// resource](https://cloud.google.com/monitoring/api/resources#tag_api).
    public var service: Swift.String = Swift.String()

    /// Initialize a new instance of `CloudEndpoints`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = CloudEndpoints().with { $0.service = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.monitoring.v3.Service.CloudEndpoints"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  /// Istio service scoped to a single Kubernetes cluster. Learn more at
  /// https://istio.io. Clusters running OSS Istio will have their services
  /// ingested as this type.
  public struct ClusterIstio: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// The location of the Kubernetes cluster in which this Istio service is
    /// defined. Corresponds to the `location` resource label in `k8s_cluster`
    /// resources.
    public var location: Swift.String = Swift.String()

    /// The name of the Kubernetes cluster in which this Istio service is
    /// defined. Corresponds to the `cluster_name` resource label in
    /// `k8s_cluster` resources.
    public var clusterName: Swift.String = Swift.String()

    /// The namespace of the Istio service underlying this service. Corresponds
    /// to the `destination_service_namespace` metric label in Istio metrics.
    public var serviceNamespace: Swift.String = Swift.String()

    /// The name of the Istio service underlying this service. Corresponds to the
    /// `destination_service_name` metric label in Istio metrics.
    public var serviceName: Swift.String = Swift.String()

    /// Initialize a new instance of `ClusterIstio`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = ClusterIstio().with { $0.location = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.monitoring.v3.Service.ClusterIstio"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  /// Istio service scoped to an Istio mesh. Anthos clusters running ASM < 1.6.8
  /// will have their services ingested as this type.
  public struct MeshIstio: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// Identifier for the mesh in which this Istio service is defined.
    /// Corresponds to the `mesh_uid` metric label in Istio metrics.
    public var meshUid: Swift.String = Swift.String()

    /// The namespace of the Istio service underlying this service. Corresponds
    /// to the `destination_service_namespace` metric label in Istio metrics.
    public var serviceNamespace: Swift.String = Swift.String()

    /// The name of the Istio service underlying this service. Corresponds to the
    /// `destination_service_name` metric label in Istio metrics.
    public var serviceName: Swift.String = Swift.String()

    /// Initialize a new instance of `MeshIstio`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = MeshIstio().with { $0.meshUid = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.monitoring.v3.Service.MeshIstio"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  /// Canonical service scoped to an Istio mesh. Anthos clusters running ASM >=
  /// 1.6.8 will have their services ingested as this type.
  public struct IstioCanonicalService: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// Identifier for the Istio mesh in which this canonical service is defined.
    /// Corresponds to the `mesh_uid` metric label in
    /// [Istio metrics](https://cloud.google.com/monitoring/api/metrics_istio).
    public var meshUid: Swift.String = Swift.String()

    /// The namespace of the canonical service underlying this service.
    /// Corresponds to the `destination_canonical_service_namespace` metric
    /// label in [Istio
    /// metrics](https://cloud.google.com/monitoring/api/metrics_istio).
    public var canonicalServiceNamespace: Swift.String = Swift.String()

    /// The name of the canonical service underlying this service.
    /// Corresponds to the `destination_canonical_service_name` metric label in
    /// label in [Istio
    /// metrics](https://cloud.google.com/monitoring/api/metrics_istio).
    public var canonicalService: Swift.String = Swift.String()

    /// Initialize a new instance of `IstioCanonicalService`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = IstioCanonicalService().with { $0.meshUid = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.monitoring.v3.Service.IstioCanonicalService"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  /// Cloud Run service. Learn more at https://cloud.google.com/run.
  public struct CloudRun: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// The name of the Cloud Run service. Corresponds to the `service_name`
    /// resource label in the [`cloud_run_revision` monitored
    /// resource](https://cloud.google.com/monitoring/api/resources#tag_cloud_run_revision).
    public var serviceName: Swift.String = Swift.String()

    /// The location the service is run. Corresponds to the `location`
    /// resource label in the [`cloud_run_revision` monitored
    /// resource](https://cloud.google.com/monitoring/api/resources#tag_cloud_run_revision).
    public var location: Swift.String = Swift.String()

    /// Initialize a new instance of `CloudRun`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = CloudRun().with { $0.serviceName = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.monitoring.v3.Service.CloudRun"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  /// GKE Namespace. The field names correspond to the resource metadata labels
  /// on monitored resources that fall under a namespace (for example,
  /// `k8s_container` or `k8s_pod`).
  public struct GkeNamespace: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// Output only. The project this resource lives in. For legacy services
    /// migrated from the `Custom` type, this may be a distinct project from the
    /// one parenting the service itself.
    public var projectId: Swift.String = Swift.String()

    /// The location of the parent cluster. This may be a zone or region.
    public var location: Swift.String = Swift.String()

    /// The name of the parent cluster.
    public var clusterName: Swift.String = Swift.String()

    /// The name of this namespace.
    public var namespaceName: Swift.String = Swift.String()

    /// Initialize a new instance of `GkeNamespace`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = GkeNamespace().with { $0.projectId = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.monitoring.v3.Service.GkeNamespace"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  /// A GKE Workload (Deployment, StatefulSet, etc). The field names correspond
  /// to the metadata labels on monitored resources that fall under a workload
  /// (for example, `k8s_container` or `k8s_pod`).
  public struct GkeWorkload: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// Output only. The project this resource lives in. For legacy services
    /// migrated from the `Custom` type, this may be a distinct project from the
    /// one parenting the service itself.
    public var projectId: Swift.String = Swift.String()

    /// The location of the parent cluster. This may be a zone or region.
    public var location: Swift.String = Swift.String()

    /// The name of the parent cluster.
    public var clusterName: Swift.String = Swift.String()

    /// The name of the parent namespace.
    public var namespaceName: Swift.String = Swift.String()

    /// The type of this workload (for example, "Deployment" or "DaemonSet")
    public var topLevelControllerType: Swift.String = Swift.String()

    /// The name of this workload.
    public var topLevelControllerName: Swift.String = Swift.String()

    /// Initialize a new instance of `GkeWorkload`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = GkeWorkload().with { $0.projectId = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.monitoring.v3.Service.GkeWorkload"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  /// GKE Service. The "service" here represents a
  /// [Kubernetes service
  /// object](https://kubernetes.io/docs/concepts/services-networking/service).
  /// The field names correspond to the resource labels on [`k8s_service`
  /// monitored
  /// resources](https://cloud.google.com/monitoring/api/resources#tag_k8s_service).
  public struct GkeService: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// Output only. The project this resource lives in. For legacy services
    /// migrated from the `Custom` type, this may be a distinct project from the
    /// one parenting the service itself.
    public var projectId: Swift.String = Swift.String()

    /// The location of the parent cluster. This may be a zone or region.
    public var location: Swift.String = Swift.String()

    /// The name of the parent cluster.
    public var clusterName: Swift.String = Swift.String()

    /// The name of the parent namespace.
    public var namespaceName: Swift.String = Swift.String()

    /// The name of this service.
    public var serviceName: Swift.String = Swift.String()

    /// Initialize a new instance of `GkeService`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = GkeService().with { $0.projectId = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.monitoring.v3.Service.GkeService"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  /// A well-known service type, defined by its service type and service labels.
  /// Documentation and examples
  /// [here](https://cloud.google.com/stackdriver/docs/solutions/slo-monitoring/api/api-structures#basic-svc-w-basic-sli).
  public struct BasicService: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// The type of service that this basic service defines, e.g.
    /// APP_ENGINE service type.
    /// Documentation and valid values
    /// [here](https://cloud.google.com/stackdriver/docs/solutions/slo-monitoring/api/api-structures#basic-svc-w-basic-sli).
    public var serviceType: Swift.String = Swift.String()

    /// Labels that specify the resource that emits the monitoring data which
    /// is used for SLO reporting of this `Service`.
    /// Documentation and valid values for given service types
    /// [here](https://cloud.google.com/stackdriver/docs/solutions/slo-monitoring/api/api-structures#basic-svc-w-basic-sli).
    public var serviceLabels: [Swift.String: Swift.String] = [:]

    /// Initialize a new instance of `BasicService`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = BasicService().with { $0.serviceType = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.monitoring.v3.Service.BasicService"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  /// Configuration for how to query telemetry on a Service.
  public struct Telemetry: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// The full name of the resource that defines this service. Formatted as
    /// described in https://cloud.google.com/apis/design/resource_names.
    public var resourceName: Swift.String = Swift.String()

    /// Initialize a new instance of `Telemetry`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = Telemetry().with { $0.resourceName = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.monitoring.v3.Service.Telemetry"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  /// REQUIRED. Service-identifying atoms specifying the underlying service.
  public enum OneOf_Identifier: Codable, Equatable, Sendable {
    /// Custom service type.
    indirect case custom(Service.Custom?)
    /// Type used for App Engine services.
    indirect case appEngine(Service.AppEngine?)
    /// Type used for Cloud Endpoints services.
    indirect case cloudEndpoints(Service.CloudEndpoints?)
    /// Type used for Istio services that live in a Kubernetes cluster.
    indirect case clusterIstio(Service.ClusterIstio?)
    /// Type used for Istio services scoped to an Istio mesh.
    indirect case meshIstio(Service.MeshIstio?)
    /// Type used for canonical services scoped to an Istio mesh.
    /// Metrics for Istio are
    /// [documented here](https://istio.io/latest/docs/reference/config/metrics/)
    indirect case istioCanonicalService(Service.IstioCanonicalService?)
    /// Type used for Cloud Run services.
    indirect case cloudRun(Service.CloudRun?)
    /// Type used for GKE Namespaces.
    indirect case gkeNamespace(Service.GkeNamespace?)
    /// Type used for GKE Workloads.
    indirect case gkeWorkload(Service.GkeWorkload?)
    /// Type used for GKE Services (the Kubernetes concept of a service).
    indirect case gkeService(Service.GkeService?)
  }

  public static var _anyTypeUrl: Swift.String {
    return "type.googleapis.com/google.monitoring.v3.Service"
  }
  public init(fromAny any: GoogleCloudWKT.`Any`) throws {
    self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
  }
  public func _pack() throws -> GoogleCloudWKT.Struct {
    return try GoogleCloudWKT._slowAnySerialize(message: self)
  }
}
